---
schema: ai6.ticket.v1
id: AI6-046
title: "Execution-Home und Änderungsausgang für Implementierungs- und Fixturns"
status: done
depends_on: [AI6-019, AI6-032]
kind: fix
milestone: M6
risk: high
files:
  - "app/AI6/Runs/RunImplementation.php"
  - "app/AI6/Agents/ExecutionHomeManager.php"
  - "app/AI6/Agents/ExecutionHome.php"
  - "README.md"
  - "config/ai6.php"
  - "docker-compose.yml"
  - "app/AI6/Runs/RunPreflight.php"
  - "app/AI6/Reviews/ReviewRound.php"
  - "app/AI6/Reviews/FindingVerificationRound.php"
  - "app/AI6/Reviews/SecurityReviewStep.php"
  - "app/AI6/Agents/FakeAgentAdapter.php"
spec_refs:
  - "docs/AI6_IMPLEMENTATION_PLAN.md — AGT-009"
  - "docs/AI6_IMPLEMENTATION_PLAN.md — GIT-010"
  - "docs/AI6_IMPLEMENTATION_PLAN.md — RUN-003"
  - "docs/AI6_IMPLEMENTATION_PLAN.md — SEC-005"
---

# AI6-046 — Execution-Home und Änderungsausgang für Implementierungs- und Fixturns

## Goal

Implementierungs- und Fixturns dieselbe serverseitig gebundene Execution-Home-Erzeugung wie Reviewturns geben, mit einem beschreibbaren Änderungsausgang, aus dem der Worker den validierten Patch importiert.

Nach diesem Ticket erzeugt `RunImplementation` für Implementierung und Fix ein Home über `ExecutionHomeManager`, dessen Workspace als einziger beschreibbarer Teil unter der Ausgabewurzel liegt, während Home, Instruktions-Overlay, Runtimeprofil und Authprojektion versiegelt bleiben. Der Worker ermittelt den Patch mit `RunPatchImporter::partition()` aus genau diesem Workspace.

## Context

Auf ausdrücklichen menschlichen Auftrag vom 6. September 2026 entfallen sämtliche noch offenen Nachweispflichten dieses Tickets, einschließlich zusätzlicher Test-, POSIX-, Prozess-, Qualitätscheck- und Release-Gate-Nachweise. Plan V1.7.6 hält diesen ausschließlich für AI6-046 geltenden Verzicht fest. Es gibt damit keine offene Nachweisvoraussetzung für die Umsetzung oder Abnahme dieses Tickets. Die funktionalen Anforderungen und Sicherheitskontrollen bleiben verbindlich. Die Entfernung der Lücke `AI6-032/AC-04` ist kein Liefergegenstand mehr; das übergeordnete Release-Gate wird durch diesen Verzicht weder geschlossen noch als bestanden ausgewiesen. Der Nutzer hat `AI6-046/AC-04` als abgeschlossen gemeldet; dies ist eine Nutzerangabe und kein hier neu erhobener Testnachweis.

Beide Abhängigkeiten sind umgesetzt und menschlich abgenommen. Dieses Ticket wurde am 6. September 2026 auf ausdrücklichen menschlichen Auftrag erneut gegen den unveränderten integrierten Stand `2f89d8e` rebased. Das Rebase-Gate nach Plan §13.6 ist geschlossen; der Ticketstatus `ready` und alle vorhandenen AC-/TC-/MG-/EXT-IDs bleiben unverändert.

`RunImplementation::executeTurn()` exportiert heute den Runworktree mit `IsolatedTreeExporter::export()` beschreibbar nach `<Agent-Arbeitswurzel>/<Batch>/tree`, übergibt dieses Verzeichnis samt den unerreichbaren Gitmetadatenpfaden direkt an `AgentAdapter::turn()`, berechnet den Patch anschließend mit `RunPatchImporter::partition()` aus genau demselben Verzeichnis und entfernt es im `finally`. `RunFixTurn` ruft dafür `RunImplementation::executeFix()`; beide Rollen laufen also durch dieselbe Naht, und `ExecutionHomeManager::create()` wird dort nicht aufgerufen. Die Reviewpfade in `ReviewRound`, `FindingVerificationRound` und `SecurityReviewStep` tun genau das: Sie legen unter `execution_mailboxes.agent_root` und `agent_output_root` ein Invocation-Verzeichnispaar an, exportieren dorthin, rufen `create()` mit Instruktionsprofil, Snapshot, Runtimeprofil und leerer `CredentialProjection` auf, übergeben `$home->workspace` an den Adapter und räumen Home, Adapter-IO, Export und Invocation-Verzeichnisse wieder ab.

`ExecutionHomeManager::create()` prüft Credential-Revision, Runtimeprofil-Hash, Instruktionsprofil und die `agent_input_limits`, kopiert den Export nach `<root>/workspace`, wobei `.git`, `.codex`, `.claude`, `AGENTS.md`, `.mcp.json`, `mcp.json`, `.gitconfig` und `.git-credentials` ausgelassen und die Snapshotinstruktionen aus dem gebundenen Snapshot zurückgeschrieben werden, und versiegelt danach die gesamte Wurzel mit `0550`/`0440`. Die Ausgabewurzel `<outputRoot>/<Name>` trägt `01730` und enthält nur `result`, `artifacts` und `instruction-patch`, ebenfalls `01730`. Ein Implementierungsturn kann in diesem Workspace also nichts ändern; genau deshalb ist `create()` bisher nicht eingebaut. `destroy()` entfernt Wurzel und Ausgabewurzel und meldet unvollständige Bereinigung als `ExecutionHomeException`.

Prozessidentitäten: Worker und App laufen als `10001:10001`, der Agentdienst als `10002:10001`; die Volumes `ai6_agent_executions` (`0750`, im Agentcontainer read-only) und `ai6_agent_outputs` (`1730`, `noexec`) gehören `10001:10001`. Ein Verzeichnis, das nur sein Eigentümer beschreiben darf, ist für den Providerprozess der Agentrolle deshalb unbeschreibbar; beschreibbar wird es nur über die gemeinsame Gruppe. `ProcessIsolationVerifier` verlangt in der Agentrolle heute ein Arbeitsverzeichnis unterhalb der Eingabewurzel; die Anerkennung der neuen Workspace-Lage gehört zu `AI6-047`, das die Agentrolle für Turns erstmals verwendet.

`FakeAgentAdapter::turn()` schreibt im Erfolgsfall eine Beispieldatei in den übergebenen Baum, legt sein IO-Verzeichnis neben der Home-Wurzel an (`dirname($tree).'-io'`, wenn der Baum `workspace` heißt) und startet ein Kindskript über `ControlProcessRunner` mit der Agentpolicy, das Environment- und Pfadsonden ausführt und heute bereits `.codex/config.toml`, `.codex/plugins/plugin.json`, `.codex/skills/SKILL.md`, `.codex/commands/run.md`, `.claude/settings.json`, `.mcp.json`, `mcp.json`, `.gitconfig`, `.git-credentials` und `nested/AGENTS.md` im Baum sowie `AGENTS.md` in den Elternverzeichnissen abfragt. `FakeAgentReleaseGateCommand::AC_COVERAGE_GAPS` führt `AC-02` und `AC-04` von `AI6-032`; `AC-04` ist dort mit genau dieser fehlenden Versiegelung begründet. `tests/Unit/Runs/ReleaseGateCommandTest.php` erwartet die Lücken bewusst und besitzt bereits den bedingten Erfolgspfad; `tests/Fixtures/Agents/release-gate-write-audit.json` inventarisiert die direkten Schreibzugriffe der gebundenen Releasetests. `ImplementationImportIsolationTest::test_the_agent_view_carries_no_git_metadata_hook_or_host_instruction` belegt Gitmetadaten, Hook und Hostinstruktion, nicht die Wirkungslosigkeit providerspezifischer Konfiguration. Der Entscheidungsantrag `docs/AI6-032_FOLGEAUFTRAG_IMPLEMENTIERUNGSISOLATION.md` beschreibt diesen Umfang; Plan V1.7.5 hat ihn diesem Blueprint zugewiesen.

## Tasks

1. `ExecutionHomeManager::create()` um einen zusätzlichen Parameter mit Standardwert für die Workspace-Lage erweitern: Für Implementierung und Fix liegt die Kopie des Exports unter der Ausgabewurzel (`<outputRoot>/<Name>/workspace`), für Reviews unverändert unter der versiegelten Wurzel, sodass die Review-Workspace-Lage unverändert bleibt. Die gemeinsamen Eingabe-/Ausgabewurzeln werden in allen vier Turnpfaden mit denselben Modi `0750`/`01730` erzeugt. Der beschreibbare Workspace erhält Verzeichnisse `0770` und reguläre Dateien `0660` ohne Sticky-Bit, damit die spätere Agentrolle diese über die gemeinsame Gruppe lesen, schreiben und löschen kann. Native Snapshotdateien bleiben gemäß `AGT-009` read-only mit `0440`; `ExecutionHome->workspace` zeigt jeweils auf den tatsächlichen Pfad. Home, Instruktions-Overlay, Runtimeprofil und Authprojektion bleiben in beiden Fällen versiegelt. Keine zweite Home-Klasse und kein zweiter Manager.
2. `RunImplementation::executeTurn()` auf diese Naht umstellen, nach dem Muster der Reviewpfade: Invocation-Verzeichnispaar unter `agent_root`/`agent_output_root`, Export dorthin, `create()` mit dem gebundenen Instruktionsprofil, Snapshot, Runtimeprofil und einer `CredentialProjection` ohne Dateien mit der aktuellen Revision, Übergabe von `$home->workspace` an `turn()`. Partition, Import, Quarantäne und Instruktionsersatz lesen aus genau diesem Workspace; die unerreichbaren Gitmetadatenpfade werden weiterhin übergeben. Die heutige Export-nach-`tree`-Kopie entfällt ersatzlos.
3. Bindungen vor Start und Resume unverändert über die vorhandenen Prüfungen (`InstructionBindingVerifier`, Preflight, die Prüfungen in `create()`) sicherstellen. Schlägt `create()` fehl, endet der Schritt benannt mit `implementation_home_unavailable`, ohne dass der Adapter startet. Keine zweite Discovery-, Credential- oder Scopepolicy.
4. Cleanup wie in `ReviewRound::destroy()`: nach Ergebnisübernahme, Fehler, Timeout und Abbruch Home, Adapter-IO, Export und Invocation-Verzeichnisse entfernen; eine unvollständige Bereinigung ist ein benannter Fehler und kein Erfolg.
5. README um Workspace-Lage und Rechte ergänzen. Zusätzliche Sonden, Tests, Testinventare und eine Änderung des Release-Gates werden für dieses Ticket nicht mehr verlangt.

## Acceptance Criteria

- [ ] **AC-01** Ein Implementierungsturn läuft in einem Home aus `ExecutionHomeManager`: Der Workspace liegt unter der Ausgabewurzel mit Verzeichnissen `0770` und regulären Dateien `0660`; native Snapshotdateien bleiben gemäß `AGT-009` read-only mit `0440`. Home, Overlay, Runtimeprofil und Auth sind read-only, und der Worker importiert die Änderung des Fake-Agenten mit `RunPatchImporter::partition()` aus genau diesem Workspace. Session-, Baum-, Diff- und Statusbindungen des Erfolgsfalls bleiben gegenüber heute unverändert.
- [ ] **AC-02** Ein Fixturn läuft über dieselbe Naht; die zweite Fixrunde erzeugt aus dem gebundenen Kontextpaket einen echten Diff, und Review-Homes bleiben vollständig versiegelt.
- [ ] **AC-03** Veraltete Credential-Revision, nicht serverbundenes Instruktionsprofil oder eine nicht verfügbare Wurzel beendet den Schritt benannt mit `implementation_home_unavailable`; die vorgelagerte Runtimebindung behält ihre bestehenden Gründe `runtime_profile_binding_missing`, `runtime_profile_not_server_bound` und `runtime_profile_drift`; der Adapter startet nicht, und es bleibt kein Home zurück.
- [ ] **AC-04** Nach Erfolg, Adapterfehler, Timeout und Abbruch existieren weder Home, Adapter-IO, Export noch Invocation-Verzeichnisse; eine gezielt blockierte Bereinigung endet beim noch laufenden Schritt als benannter Fehler und nicht als erfolgreicher Schritt. Bei bereits geparktem oder beendetem Schritt wird der Cleanup-Fehler als Ereignis festgehalten, ohne Job oder Run nachträglich umzusetzen.
- [ ] **AC-05** Unter `.codex/` und `.claude/` angelegte Konfiguration, Hook-, Plugin-, Skill-, Command- und Helferdateien sowie `.mcp.json` sind im Prozess des Fake-Agenten weder im Workspace noch in dessen Elternverzeichnissen lesbar oder ausführbar, während reguläre Projektdateien unverändert vorhanden sind.
- [ ] **AC-06** Die bestehenden Verträge bleiben unverändert: keine Gitmetadaten, Hooks oder Hostinstruktionen in der Agentensicht, keine erreichbaren Credentials oder Worktree, unveränderter Snapshot, Retry, Resume und Scope-Quarantäne.
- [ ] **AC-07** README beschreibt die Workspace-Lage und ihre Rechte. Die bisher zusätzlich verlangte Schließung von `AI6-032/AC-04` entfällt durch den menschlichen Nachweisverzicht.

## Test Cases

Die veröffentlichten IDs bleiben zur Rückverfolgbarkeit erhalten. Die folgenden Einträge begründen keine offenen Testaufträge und behaupten keinen bestandenen Testlauf.

- **TC-01** Nachweispflicht für den Implementierungsturn auf menschlichen Auftrag aufgehoben.
- **TC-02** Nachweispflicht für Fixturns und Review-Homes auf menschlichen Auftrag aufgehoben.
- **TC-03** Nachweispflicht für Bindungsfehler und ausgebliebenen Adapterstart auf menschlichen Auftrag aufgehoben.
- **TC-04** Der zugehörige Cleanup-Vertrag `AC-04` wurde vom Nutzer als abgeschlossen gemeldet. Keine erneute Nachweispflicht; kein unabhängig erhobenes Testergebnis wird behauptet.
- **TC-05** Zusätzlicher Prozess-, Discovery- und Gegenprobennachweis auf menschlichen Auftrag aufgehoben.
- **TC-06** Gesonderte Regressions- und POSIX-Nachweispflicht für dieses Ticket auf menschlichen Auftrag aufgehoben.
- **TC-07** Release-Gate-, Schreibinventar- und Dokumentationsnachweispflicht auf menschlichen Auftrag aufgehoben.

## AC Coverage

Historische Zuordnung gemäß der Ausnahme in Plan V1.7.6: Die referenzierten TC-Einträge dokumentieren den Nachweisverzicht beziehungsweise die Nutzerangabe, keine offene Evidenzpflicht und keinen Testpass.

| AC | Evidence |
|---|---|
| AC-01 | TC-01 |
| AC-02 | TC-02 |
| AC-03 | TC-03 |
| AC-04 | TC-04 |
| AC-05 | TC-05 |
| AC-06 | TC-06 |
| AC-07 | TC-07 |

## Initial Scope and Sensitive Paths

**Expected initial scope:**

- `app/AI6/Runs/RunImplementation.php` — existing
- `app/AI6/Agents/ExecutionHomeManager.php` — existing
- `app/AI6/Agents/ExecutionHome.php` — existing
- `README.md` — existing
- `config/ai6.php` — existing
- `docker-compose.yml` — existing
- `app/AI6/Runs/RunPreflight.php` — existing
- `app/AI6/Reviews/ReviewRound.php` — existing
- `app/AI6/Reviews/FindingVerificationRound.php` — existing
- `app/AI6/Reviews/SecurityReviewStep.php` — existing
- `app/AI6/Agents/FakeAgentAdapter.php` — existing

**Sensitive paths:**

- `app/AI6/Agents/ExecutionHomeManager.php` — erwartete Entscheidung: nur die beschreibbare Workspace-Lage mit ihren Gruppenrechten; Filterliste, Versiegelung und Prüfungen des Homes werden nicht gelockert.

## Do Not Change

- `app/AI6/Agents/AgentAdapter.php` — der Adaptervertrag bleibt unverändert; die Übergabe des Turns in die Agentrolle ist `AI6-047`.
- `app/AI6/Git/RunPatchImporter.php`, `app/AI6/Git/IsolatedTreeExporter.php` — Export- und Patchimportvertrag werden konsumiert, nicht geändert.
- `app/AI6/Reviews/` — die Review-Workspace-Lage und Versiegelung bleiben unverändert; ausschließlich die Erzeugung der gemeinsamen Wurzeln verwendet wie der Implementierungspfad `0750`/`01730`.
- `app/AI6/Shared/Process/`, `app/AI6/Shared/Security/` — keine Änderung an Prozessnaht, Isolationsgrenze oder Sicherheitsmaßnahmen; die Anerkennung der neuen Workspace-Lage durch `ProcessIsolationVerifier` gehört zu `AI6-047`.
- `database/migrations/`, `composer.json`, `composer.lock` — keine Migration und keine Abhängigkeit.
- `docs/AI6_IMPLEMENTATION_PLAN.md`, `AGENTS.md`, `CLAUDE.md`, `tickets/` — der Implementierungsagent ändert weder diese Quellen noch Ticketstatus oder Metadaten; auch `tickets/AI6-032.md` bleibt unverändert.

## Out of Scope

- Reale Providertransporte, native Konfiguration und Discovery (`AI6-033`, `AI6-034`, `AI6-041`, `AI6-042`).
- Die Übergabe des Turns in die Agentrolle über die Mailbox samt Anpassung der Isolationsprüfung an die neue Workspace-Lage (`AI6-047`).
- Persistenter Credential-Store und Authprojektion mit Dateien (`AI6-035`).
- Die Lücke `AC-02` von `AI6-032`.

## Manual and External Gates

None.

## Review Focus

- Liest der Worker Partition, Import, Quarantäne und Instruktionsersatz wirklich aus `$home->workspace`, oder existiert noch eine zweite Kopie?
- Bleibt die versiegelte Wurzel read-only, während nur der Workspace unter der Ausgabewurzel beschreibbar ist, und tragen seine Verzeichnisse und Dateien die Gruppenrechte, die die Agentrolle später braucht?
- Bleiben reguläre Projektdateien erhalten und die funktionalen Sicherheitsverträge wirksam?

## Notes

- Review-Korrektur auf ausdrücklichen menschlichen Auftrag vom 6. September 2026: Die gemeinsame Wurzelerzeugung toleriert konkurrierendes Anlegen und setzt Rechte nur nach eigener erfolgreicher Erzeugung. Doppelte Scope-Einträge wurden entfernt; Aufgabe 1 und AC-01 präzisieren die unveränderte Ausnahme für native Snapshotdateien (`0440`, `AGT-009`). IDs und Ticketstatus bleiben unverändert.

- Scope-/Vertragskorrektur auf ausdrücklichen Auftrag zur Behebung der acht Review-Findings vom 6. September 2026: `config/ai6.php` nimmt bereits in AI6-046 beide Agentwurzeln in die Prozesspolicy auf, weil der Fake-Turn den Änderungsausgang als Arbeitsverzeichnis nutzt; `RunPreflight.php` prüft beide Wurzeln. Nur die rollenbezogene Isolationsprüfung und Mailboxübergabe bleiben AI6-047. `docker-compose.yml` begrenzt das Agent-Ausgabevolume auf 1 GiB. Die drei Reviewpfade vereinheitlichen die Erzeugungsmodi der gemeinsamen Wurzeln. `FakeAgentAdapter.php` und `tests/Feature/Git/ImplementationImportIsolationTest.php` ergänzen die Positivkontrolle und korrigieren die Host-Instruktionsposition; `tests/Feature/Runs/ExecutionJobContractTest.php` und `BuildsReviewOnlyRunFixture.php` bilden die vollständige Preflightpolicy ab. Diese Erweiterungen sind durch den Review-Fix-Auftrag einschließlich bedarfsgerechter Ticketanpassung freigegeben; Ticketstatus und Gateentscheidungen werden dadurch nicht geändert.

- Definition of Done: `docs/AI6_IMPLEMENTATION_PLAN.md` §12.2.
- Ticket status, approval and run metadata are owned by AI6. The implementation agent never changes them.
- Necessary deviations are reported as `needs_human` or as a scope/contract request, never applied silently.
- Am 6. September 2026 auf ausdrücklichen menschlichen Auftrag erneut gegen den unveränderten integrierten Stand `2f89d8e` rebased, nachdem `AI6-019` und `AI6-032` umgesetzt und menschlich abgenommen waren. Verifiziert wurden die bestehenden `ExecutionHomeManager::create()`-/`destroy()`- und `ExecutionHome`-Verträge, die direkte Export-/Import-Naht in `RunImplementation::executeTurn()` und `executeFix()`, das Review-Home-Muster in `ReviewRound`, `FindingVerificationRound` und `SecurityReviewStep`, die Verträge von `RunPatchImporter::partition()` und `IsolatedTreeExporter::export()`, die FakeAgent-/Release-Gate-Nähte sowie sämtliche aufgeführten Test- und Dokumentationspfade einschließlich ihrer Scope-Marker. Das Rebase-Gate ist geschlossen; das Ticket bleibt nach Plan §13.7 unveröffentlicht, der Status bleibt `ready`, und AC-/TC-/MG-/EXT-IDs sind unverändert.
- Vertragsrevision am 6. September 2026 auf ausdrücklichen menschlichen Auftrag: Offene Nachweispflichten einschließlich Linux-/POSIX-Nachweisen und der Schließung von `AI6-032/AC-04` sind für dieses Ticket aufgehoben. Die veröffentlichten AC-/TC-IDs werden weder umnummeriert noch wiederverwendet. Vorhandene Tests und Laufzeitkontrollen werden durch diese Dokumentationsänderung nicht verändert.
