---
schema: ai6.ticket.v1
id: AI6-029
title: "Finaler Commit, Ticketstatus, Push, Drift und Cleanup"
status: todo
depends_on: [AI6-009, AI6-014, AI6-027, AI6-028]
kind: feature
milestone: M5
risk: high
files:
  - "app/AI6/Git/"
  - "app/AI6/Runs/"
  - "app/AI6/Tickets/"
  - "app/AI6/HumanLoop/"
  - "app/AI6/Shared/AI6ServiceProvider.php"
  - "database/migrations/"
  - "tests/Unit/Git/"
  - "tests/Unit/Runs/"
  - "tests/Unit/Tickets/"
  - "tests/Feature/Git/"
  - "tests/Feature/Runs/"
  - "tests/Feature/HumanLoop/"
  - "tests/Unit/ScaffoldStructureTest.php"
  - "README.md"
spec_refs:
  - "docs/AI6_IMPLEMENTATION_PLAN.md — GIT-005"
  - "docs/AI6_IMPLEMENTATION_PLAN.md — GIT-006"
  - "docs/AI6_IMPLEMENTATION_PLAN.md — GIT-007"
  - "docs/AI6_IMPLEMENTATION_PLAN.md — GIT-008"
  - "docs/AI6_IMPLEMENTATION_PLAN.md — TKT-005"
  - "docs/AI6_IMPLEMENTATION_PLAN.md — TKT-012"
  - "docs/AI6_IMPLEMENTATION_PLAN.md — RUN-005"
  - "docs/AI6_IMPLEMENTATION_PLAN.md — RUN-009"
---

# AI6-029 — Finaler Commit, Ticketstatus, Push, Drift und Cleanup

## Goal

Den akzeptierten Candidate unverändert committen, gemäß Pushpolicy veröffentlichen und den Run sauber abschließen.

Nach diesem Ticket endet ein erfolgreicher Run in genau einem veröffentlichten Arbeitsbranch und genau einem autorisierten Ticketstatuscommit. Ein Reviewer erkennt den Erfolg daran, dass der finale Commit dieselbe Tree-OID trägt wie der geprüfte Candidate und genau das aktuelle `run_base_sha` als einzigen Parent besitzt, dass ein bestätigter No-change-Lauf stattdessen einen Run-Branchref exakt auf `run_base_sha` veröffentlicht und keinen leeren Codecommit erzeugt, dass der anschließende Control-Branch-Compare-and-Swap ausschließlich die autorisierte Statusänderung und den gebundenen `## Recorded Scope`-Block derselben Ticketdatei enthält und `ticket_contract_sha256` davor und danach identisch lässt, und dass ein Absturz an jeder Phase dieser Kette weder einen zweiten Push noch einen zweiten Runabschluss noch eine doppelte Sperrfreigabe erzeugt.

## Context

Dieses Ticket wurde auf ausdrückliche menschliche Anordnung vor der Umsetzung von `AI6-027` und `AI6-028` erzeugt und ist damit `ahead-derived` nach Plan §13.6. `AI6-009` und `AI6-014` sind umgesetzt und menschlich abgenommen; ihre Nähte wurden im Stand `cf4183a` verifiziert. Offen sind ausschließlich `AI6-027` und `AI6-028`.

Noch nicht vorhanden und ausschließlich durch diese beiden Tickets entstehend sind: der Publish-Kandidat mit seiner Tree-OID-, Diff-Hash- und `candidate_base_sha`-Bindung, die Finalcheckphase, der Provenienz-Preflight, das zentrale Candidate-Gate, der Wartestatus für offene manuelle Gates sowie das gebundene Sicherheitsergebnis und sein Wartestatus. Dieses Ticket beschreibt keine dieser Nähte als bereits verifiziert; es benennt sie ausschließlich mit den Namen, die der Plan selbst verwendet, und leitet vor `status: ready` jede konkrete Klasse, jeden Command und jeden Testnamen gegen den dann realen Repositoryzustand neu ab.

Real vorhanden und im Stand `cf4183a` verifiziert ist der gesamte Git- und Sagaunterbau. `App\AI6\Git\HardenedGitRunner` liefert bereits `planSingleFileMutation()`, `writeSingleFileMutation()`, `createSingleParentCommit()`, `inspectSingleParentCommit()`, `isAncestor()`, `updateRef()`, `pushCommitCas()`, `probeRemote()`, `resolveRef()`, `resolveRunBranch()`, `resolveFirstParent()`, `canonicalRawDiff()`, `deleteRunBranch()`, `removeRunWorktree()`, `pruneWorktrees()` und `discardRunWorkspace()`. `inspectSingleParentCommit()` prüft dabei schon heute, dass ein vorbereiteter Commit genau einen Parent und den gebundenen Tree besitzt, und `pushCommitCas()` veröffentlicht ausschließlich mit erwarteter Remote-OID. `App\AI6\Git\GitRemotePolicy` mit `validate()` und `validateRef()` ist die eine Ref- und Remoteprüfung; die Refmuster stehen in `config/ai6.php` unter `git.allowed_ref_patterns` und `control_operations.managed_ref_allowlist`. `App\AI6\Git\RunBranchName` bindet jeden Run-Branch auf den Namensraum `refs/heads/ai6/runs/` mit Projektkennung und Run-ID. `App\AI6\Git\TicketFileDeltaProof::assertOnlyPathChanged()` beweist bereits über den realen Objektstore, dass ein Commit sich von seinem Parent in exakt einem gebundenen Pfad unterscheidet.

Die Statussaga ist real vorgezeichnet und wird über ihre kleinen vorhandenen Nähte weiterverwendet. `App\AI6\Git\TicketMutationExecutor` führt mit `advance()`, `activeIntentUnderLock()`, `cleanupFailedAttempt()`, `recoveryFinding()`, `retryRecovery()`, `adoptExternalState()` und `abandon()` die phasenweise, absturzsichere Ticketmutation samt `App\AI6\Git\Models\TicketMutation`. `App\AI6\Runs\ReportOnlyCompletionService` mit `start()`, `confirm()`, `reconcileOperation()`, `recordConflict()` und `resolveStatusConflict()` zeigt den konkreten report-only Abschluss, `App\AI6\Runs\RunCancellationService` den zweiten Statusoperationsverbraucher. Dieses Ticket legt für den Publishabschluss einen eigenen konkreten Dienst an und teilt nur den vorhandenen Ticketmutations-, Intent- und Reconciliation-Unterbau; eine generische Basissaga oder abstrakte Servicehierarchie entsteht nicht. Der Rundatensatz `App\AI6\Runs\Models\Run` führt bereits `status_operation_id`, `pending_status_operation_id` und `confirmed_branch_publication_oid`; letzteres ist die aus `AI6-026` stammende Vorbedingung des Abbruchverbots nach bestätigter Veröffentlichung, deren Producer ausdrücklich in diesem Ticket entsteht.

Die Ticketstatusseite ist ebenfalls verifiziert und unvollständig. `App\AI6\Tickets\TicketStatusOperation` führt heute `approve`, `block`, `cancel`, `return_to_todo`, `complete_review` und `complete_report_only`; ein Übergang `in_progress → review` existiert nicht und gehört nach Plan §5.2 ausschließlich diesem Ticket. `App\AI6\Tickets\TicketStatusTransitionPolicy` zeigt zugleich das Muster, dem er folgen muss: `complete_report_only` wird im generischen Pfad mit einem eigenen Fehlercode abgewiesen und ist ausschließlich über seine gebundene Saga zulässig. `App\AI6\Tickets\TicketContractHasher` nimmt den AI6-eigenen Abschnitt `## Recorded Scope` bereits aus dem kanonischen Vertragshash aus; die Rückschreibung dieses Abschnitts nach `TKT-012` ist damit hashneutral vorbereitet, aber noch ohne Schreiber.

Für den wirksamen Scope liegen die Daten bereits vollständig vor: `App\AI6\Runs\Models\ScopeDecision` mit `path`, `outcome`, `reason` und `human_request_id`, `App\AI6\Runs\EffectiveScope`, `App\AI6\Runs\ScopeCategorizer` mit `App\AI6\Runs\ScopeCategory`, das Feld `added_scope_paths_count` am Run, die Quarantäneartefakte über `App\AI6\Runs\RunArtifactKind::QUARANTINED_PATH` und die Limitauflösung in `App\AI6\Runs\RunLimitPolicy`. Dieses Ticket erzeugt daraus die Dokumentation, nicht die Entscheidungen.

Zwei reale Randbedingungen sind für die Registrierung entscheidend. Erstens ist `App\AI6\Runs\WaitReason::MANUAL_PUSH` als Enumwert vorhanden, im Register in `app/AI6/Shared/AI6ServiceProvider.php` aber nicht eingetragen. Zweitens ist `status_sync` dort bereits eingetragen — mit dem Producer der report-only Abschluss-Saga und dem Resolver für den erneuten Compare-and-Swap. `App\AI6\Runs\WaitReasonRegistry::register()` wirft bei einer abweichenden Zweitregistrierung desselben Grundes einen benannten Konflikt und lässt den Bootstrap fehlschlagen. Der Post-Push-Fall dieses Tickets muss deshalb die vorhandene Registrierung dieses einen Grundes erweitern statt eine zweite, widersprüchliche anzulegen. Die Pushmodi sind mit `manual` und `automatic_after_gates` bereits an zwei Stellen geschlossen validiert: in `App\AI6\Projects\ProjectConfigurationParser` und in `App\AI6\Runs\TicketApprovalController`.

Zur Größe dieses Tickets nach Plan §13.2, ausdrücklich und nicht versteckt: Wesentlich verändert wird genau ein fachlicher Ablauf, nämlich der Runabschluss aus Commit, Push, Statussynchronisation und Cleanup. Seine Bestandteile sind nicht unabhängig lieferbar: Ein Push ohne Statussynchronisation ließe das Projekt dauerhaft gesperrt zurück, eine Statussynchronisation ohne bestätigten Branchref widerspräche `GIT-008`, und ein Wartegrund ohne Resolver lässt den Bootstrap fehlschlagen. Die Ausnahme aus Plan §13.2 wird für diese eine atomare Abschlusskette in Anspruch genommen.

Remoteantwort, Ticketmarkdown, Providertext und Projektkonfiguration bleiben untrusted Evidenz. Weder ein Pushmodus noch eine Refwahl, eine Rolle oder ein Statusziel folgt aus ihnen.

## Tasks

1. Den finalen Commit des geänderten Candidate erzeugen: Die Checkpoints des Run-Branch werden zu genau einem Ticket-Commit zusammengeführt, dessen Tree-OID identisch mit der gebundenen Candidate-Tree-OID ist und dessen einziger Parent exakt das aktuelle `run_base_sha` ist. Der Nachweis beider Eigenschaften läuft über die vorhandene Commitinspektion am realen Objektstore und nicht über eine gemeldete Angabe.
2. Den bestätigten No-change-Zweig ohne Codecommit umsetzen: Ist der tatsächliche Diff leer und sind Checks, AC-Abdeckung, Reviews und Gates bestätigt, entsteht kein künstlicher leerer Commit. Stattdessen wird der lokale Run-Branchref exakt auf `run_base_sha` gesetzt und derselbe Ref unter erwarteter Remote-OID veröffentlicht beziehungsweise als bereits identisch bestätigt.
3. Die Pushpolicy anschließen: Der gebundene Pushmodus ist exakt `manual` oder `automatic_after_gates`; jeder andere, verkürzte oder aus Projektinhalt stammende Wert wird abgewiesen. Der manuelle Modus erzeugt den Wartestatus `manual_push`, der automatische Modus veröffentlicht erst nach allen Gates.
4. Jede Veröffentlichung an erwartete Remote-OID und Branch-Allowlist binden: Es wird ausschließlich der Run-Branch dieses Runs veröffentlicht, ausschließlich unter der erwarteten Remote-OID und ausschließlich innerhalb der vorhandenen Ref- und Remoteprüfung. Ein fremder Branch ist kein zulässiges Ziel.
5. Den `manual_push`-Producer gemeinsam mit seinem Resolver und dem Cancelpfad im vorhandenen Register eintragen: Der einzige Resolver ist die autorisierte Pushaktion auf unverändertem Candidate; Cancel bleibt der ausdrückliche zweite Weg.
6. Den Post-Push-Fall von `status_sync` in die bereits vorhandene Registrierung dieses einen Grundes aufnehmen, statt eine zweite widersprüchliche Registrierung anzulegen: Producer und Resolver werden gemeinsam so formuliert, dass sie den report-only Abschluss und die Post-Push-Synchronisation abdecken und der Bootstrap konfliktfrei bleibt.
7. Neuen Ticketstatusübergang `in_progress → review` als eigene Statusoperation einführen, die ausschließlich der Post-Push-Statussaga gehört: Der generische Pfad weist sie wie den report-only Übergang mit einem benannten Fehlercode ab, und keine UI kann ein Statusziel frei übergeben.
8. Die Post-Push-Statussaga als konkreten Abschlussdienst absturzsicher umsetzen: Erwarteter Parent, Zielblob beziehungsweise Zieltree und beabsichtigter Commit werden vor dem Schreiben persistiert und an `status_operation_id` gebunden; der Compare-and-Swap läuft gegen den frisch verifizierten Control-Head, und erst der bestätigte eigene Commit erlaubt Runabschluss und Sperrfreigabe. Sie verwendet den vorhandenen Ticketmutations-, Intent- und Reconciliation-Unterbau; eine generische Saga-Abstraktion entsteht nicht.
9. Die Rückschreibung des wirksamen Scopes nach `TKT-012` in genau diesem einen Status-Compare-and-Swap umsetzen: Der AI6-eigene Abschnitt `## Recorded Scope` mit initialem Scope, je aufgenommenem Pfad dem benannten Entscheidungsgrund, den quarantänierten Pfaden und dem verbrauchten Anteil des Pfadlimits wird redigiert in dieselbe Ticketdatei geschrieben. Er ist kein Contract Amendment, invalidiert keine Reviewevidenz und wird niemals von einem Agenten geschrieben.
10. Die Unversehrtheit des Statuscommits beweisen: Der Commit enthält ausschließlich die autorisierte Statusänderung und den gebundenen `## Recorded Scope`-Block derselben Ticketdatei; jeder andere Treeeintrag bleibt bytegleich, und `ticket_contract_sha256` ist vor und nach dem Compare-and-Swap identisch. Der Nachweis läuft über die vorhandene Deltaprüfung am realen Objektstore.
11. Die Reihenfolge unverrückbar machen: Vor bestätigtem Branchref findet keine Statussynchronisation statt; schlägt die Statussynchronisation fehl, bleibt das Projekt gesperrt, und der bereits bestätigte Branch-Push wird nicht wiederholt. Nach bestätigtem Branch-Push ist generischer Abbruch gesperrt.
12. Die Reconciliation auf den eigenen Commit begrenzen: Ein Absturz nach bestätigtem Status-Compare-and-Swap und vor Runabschluss oder Sperrfreigabe versöhnt ausschließlich den eigenen persistierten Commit. Ein fremder, ähnlich aussehender Statuscommit wird nicht übernommen.
13. Den unklaren Pushausgang idempotent behandeln: Ein Retry nach unklarem Ergebnis prüft zuerst den Remotezustand und pusht nicht blind ein zweites Mal; ein bereits identischer Remoteref gilt als bestätigt.
14. Die Blockade bei ungültiger Evidenz durchsetzen: Kein finaler Commit und kein Push entstehen bei ungültigem oder veraltetem Security- beziehungsweise Reviewresultat oder bei einem offenen beziehungsweise stale gewordenen `MG-`/`EXT-`-Gate.
15. Basisdrift verständlich blockieren: Eine Änderung der Runbasis oder des Control-Heads zwischen Candidate und Commit führt über den vorhandenen Producer in den Wartestatus für Basisdrift; ein stilles Rebase findet nicht statt.
16. Das Cleanup für Erfolg und Fehler umsetzen: Worktree, Run-Branchressourcen und transiente Arbeitsdaten werden über die vorhandene Workspace-Lebenszyklusnaht idempotent abgeräumt, ohne den veröffentlichten Branch oder Auditdaten zu entfernen.
17. Neue Folgemigration für Candidate-Commitbindung, Pushzustand, Post-Push-Sagabindung und Recorded-Scope-Nachweis anlegen; veröffentlichte Migrationen bleiben unverändert, und es entsteht kein zweiter Status- oder Auditspeicher.
18. Manuellen Push und Konfliktentscheidung über die vorhandene authentifizierte, autorisierte und CSRF-geschützte Human-Request-/Interventionsroute führen. Die vorhandene Projektaktion `intervene_run` und die erwartete Runversion werden wiederverwendet; keine neue Autorisierungsaktion und keine zweite Route entstehen, und kein Browserrequest führt Git direkt aus.
19. Tests anlegen und fortschreiben: Commit-, Push-, Drift- und Cleanupnachweise unter `tests/Feature/Git/`, Sagacrashmatrix, No-change-Zweig und Resolvernachweise unter `tests/Feature/Runs/`, Statusoperation und Registrierung unter `tests/Unit/Tickets/` und `tests/Unit/Runs/`, Recorded-Scope-Hashneutralität unter `tests/Unit/Tickets/` und `tests/Unit/Git/`, Interventions- und Autorisierungsnachweise unter `tests/Feature/HumanLoop/`, Scaffoldinventur fortschreiben, Registrierungen in `app/AI6/Shared/AI6ServiceProvider.php` ergänzen und `README.md` um Commitvertrag, Pushmodi, Statussaga, Recorded Scope und Cleanup erweitern.

## Acceptance Criteria

- [ ] **AC-01** Der finale Commit eines geänderten Candidate trägt exakt dessen gebundene Tree-OID und besitzt genau das aktuelle `run_base_sha` als einzigen Parent; beide Eigenschaften werden am realen Objektstore nachgewiesen, nicht aus einer gemeldeten Angabe übernommen.
- [ ] **AC-02** Nach einem bestätigten Contract Amendment ist der einzige Parent des finalen Commits das neueste `run_base_sha`; ein Commit gegen eine ältere Basis entsteht nicht.
- [ ] **AC-03** Ein durch leeren tatsächlichen Diff, Checks, AC-Abdeckung, Reviews und Gates bestätigter No-change-Lauf erzeugt keinen leeren Codecommit; stattdessen zeigt der lokale Run-Branchref exakt auf `run_base_sha`, und derselbe Ref wird unter erwarteter Remote-OID veröffentlicht oder als bereits identisch bestätigt.
- [ ] **AC-04** Der gebundene Pushmodus ist exakt `manual` oder `automatic_after_gates`; ein unbekannter, verkürzter oder aus Projektinhalt stammender Wert wird abgewiesen und führt zu keiner Veröffentlichung.
- [ ] **AC-05** Jede Veröffentlichung betrifft ausschließlich den Run-Branch dieses Runs, erfolgt ausschließlich unter der erwarteten Remote-OID und ausschließlich innerhalb der vorhandenen Ref- und Remoteprüfung; ein fremder Branch wird abgewiesen.
- [ ] **AC-06** Manueller Pushmodus setzt vor der Autorisierung den Wartestatus `manual_push`; einziger Resolver ist die autorisierte Pushaktion auf unverändertem Candidate, daneben steht ausschließlich der Cancelpfad.
- [ ] **AC-07** Der Ticketstatusübergang `in_progress → review` ist ausschließlich über die Post-Push-Statussaga möglich; der generische Statuspfad weist ihn mit einem benannten Fehlercode ab, und keine UI kann ein Statusziel frei übergeben.
- [ ] **AC-08** Die Statussynchronisation beginnt erst nach bestätigtem Branchref. Schlägt sie fehl, bleibt das Projekt gesperrt, und der bereits bestätigte Branch-Push wird nicht wiederholt.
- [ ] **AC-09** Der Statuscommit enthält ausschließlich die autorisierte Statusänderung und den gebundenen `## Recorded Scope`-Block derselben Ticketdatei; jeder andere Treeeintrag bleibt bytegleich.
- [ ] **AC-10** `ticket_contract_sha256` ist vor und nach dem Status-Compare-and-Swap identisch; die Rückschreibung des wirksamen Scopes ist kein Contract Amendment und invalidiert keine Reviewevidenz.
- [ ] **AC-11** Der zurückgeschriebene Abschnitt enthält initialen Scope, je aufgenommenem Pfad den benannten Entscheidungsgrund, die quarantänierten Pfade und den verbrauchten Anteil des Pfadlimits, ist zentral redigiert und wird von keinem Agenten geschrieben.
- [ ] **AC-12** Eine Wiederholung der Rückschreibung ist idempotent und erzeugt genau einen wirksamen Commit; ein Compare-and-Swap-Konflikt lässt sie ohne Teilwirkung ausfallen.
- [ ] **AC-13** Ein Absturz an jeder gemeinsamen Phase der Kette aus Commit, Push, Statussaga und Abschluss wird ohne doppelte Wirkung fortgesetzt: kein zweiter Commit, kein zweiter Push, genau ein Runabschluss und genau eine Sperrfreigabe.
- [ ] **AC-14** Die Reconciliation übernimmt ausschließlich den eigenen persistierten Statuscommit; ein fremder, ähnlich aussehender `review`-Commit erzeugt weder Runabschluss noch Sperrfreigabe.
- [ ] **AC-15** Ein Retry nach unklarem Pushergebnis prüft zuerst den Remotezustand; ein bereits identischer Remoteref gilt als bestätigt, und es wird kein zweites Mal gepusht.
- [ ] **AC-16** Remotedrift und ein nicht vorspulbarer Remotezustand blockieren verständlich benannt, ohne den Remoteref zu überschreiben und ohne die Runbasis still zu rebasen.
- [ ] **AC-17** Bei ungültigem oder veraltetem Security- beziehungsweise Reviewresultat sowie bei offenem oder stale gewordenem `MG-`/`EXT-`-Gate entsteht weder ein finaler Commit noch ein Push.
- [ ] **AC-18** Nach bestätigtem Branch-Push ist generischer Abbruch gesperrt; ausschließlich die Statussynchronisation setzt den Run fort, und der Push wird nicht zurückgerollt.
- [ ] **AC-19** Cleanup nach Erfolg und nach Fehler ist idempotent: Worktree, Run-Branchressourcen und transiente Arbeitsdaten werden abgeräumt, während der veröffentlichte Branch und alle Auditdaten erhalten bleiben.
- [ ] **AC-20** Manueller Push und Konfliktentscheidung laufen über die vorhandene authentifizierte, projektbezogen autorisierte und CSRF-geschützte Interventionsroute, verlangen die erwartete Runversion und führen kein Git direkt aus dem Browserrequest aus; eine neue Route oder Autorisierungsaktion entsteht nicht.
- [ ] **AC-21** Nach dem Bootstrap besitzt `manual_push` einen Producer und die Resolver autorisierte Pushaktion auf unverändertem Candidate beziehungsweise Cancel, und `status_sync` bleibt genau einmal registriert; eine zweite abweichende Registrierung desselben Grundes lässt den Bootstrap fehlschlagen.

## Test Cases

- **TC-01** Git-Integrationstest in `tests/Feature/Git/`: Nach einem normalen Lauf ist die Tree-OID des finalen Commits identisch mit der gebundenen Candidate-Tree-OID, und die Commitinspektion weist genau einen Parent nach, der exakt dem aktuellen `run_base_sha` entspricht.
- **TC-02** Git-Integrationstest in `tests/Feature/Git/`: Nach einem bestätigten Contract Amendment besitzt der finale Commit das neueste `run_base_sha` als einzigen Parent; ein Commit gegen die ältere Basis wird abgewiesen.
- **TC-03** Git-Integrationstest in `tests/Feature/Git/`: Ein bestätigter No-change-Lauf erzeugt keinen Commit; der lokale Run-Branchref zeigt exakt auf `run_base_sha`, die Veröffentlichung erfolgt unter erwarteter Remote-OID, und ein bereits identischer Remoteref wird als bestätigt gewertet.
- **TC-04** Unit-Test in `tests/Unit/Runs/`: Die Pushmodusauflösung akzeptiert exakt `manual` und `automatic_after_gates`; unbekannte, verkürzte und aus Projektinhalt stammende Werte werden mit benanntem Fehlercode abgewiesen.
- **TC-05** Git-Integrationstest in `tests/Feature/Git/`: Ein Push auf einen fremden Branch, ein Push ohne erwartete Remote-OID und ein Ref außerhalb der Allowlist werden je einzeln abgewiesen; der Remotezustand bleibt in allen drei Fällen unverändert.
- **TC-06** Feature-Test in `tests/Feature/Runs/`: Im manuellen Pushmodus steht der Run in `manual_push`; die autorisierte Pushaktion auf unverändertem Candidate setzt genau den gebundenen Schritt fort, ein veränderter Candidate wird abgewiesen, und der Cancelpfad beendet den Warteschritt ohne Veröffentlichung.
- **TC-07** Unit-Test in `tests/Unit/Tickets/`: Der Übergang `in_progress → review` wird im generischen Statuspfad mit benanntem Fehlercode abgewiesen und ist ausschließlich über die gebundene Saga zulässig; ein frei übergebenes Statusziel wird nicht akzeptiert.
- **TC-08** Feature-Test in `tests/Feature/Runs/`: Eine fehlgeschlagene Statussynchronisation nach bestätigtem Branchref lässt das Projekt gesperrt, wiederholt den Push nicht und bietet ausschließlich den erneuten Compare-and-Swap beziehungsweise die autorisierte Konfliktentscheidung an.
- **TC-09** Git-Integrationstest in `tests/Feature/Git/`: Der Statuscommit unterscheidet sich von seinem Parent in genau der einen Ticketdatei; die Deltaprüfung am realen Objektstore weist jeden anderen Treeeintrag als bytegleich nach.
- **TC-10** Unit-Test in `tests/Unit/Tickets/`: Der kanonische Ticketvertragshash ist für dieselbe Ticketdatei mit und ohne `## Recorded Scope`-Abschnitt identisch, auch bei geändertem Abschnittsinhalt und bei unterschiedlicher Zeilenendennormalisierung.
- **TC-11** Feature-Test in `tests/Feature/Runs/`: Der zurückgeschriebene Abschnitt enthält initialen Scope, je aufgenommenem Pfad den benannten Entscheidungsgrund, die quarantänierten Pfade und den verbrauchten Anteil des Pfadlimits; ein sensibler Wert erscheint ausschließlich redigiert.
- **TC-12** Git-Integrationstest in `tests/Feature/Git/`: Derselbe Status-Compare-and-Swap schreibt Status und Scope-Dokumentation in genau einem Commit; eine Wiederholung ist idempotent und erzeugt keinen zweiten Commit, und ein Compare-and-Swap-Konflikt fällt ohne Teilwirkung aus.
- **TC-13** Feature-Test in `tests/Feature/Runs/`: Post-Push-Crashmatrix mit einer Injektion an jeder gemeinsamen Phase aus Commit, Push, persistiertem Intent, Status-Compare-and-Swap, Runabschluss und Sperrfreigabe. Jeder Wiederanlauf führt zu genau einem Commit, genau einem Push, genau einem Runabschluss und genau einer Sperrfreigabe.
- **TC-14** Git-Integrationstest in `tests/Feature/Git/`: Ein fremder, ähnlich aussehender `review`-Statuscommit auf dem Control-Branch wird von der Reconciliation nicht übernommen; Run und Projektsperre bleiben bestehen, und der eigene persistierte Commit bleibt die einzige akzeptierte Evidenz.
- **TC-15** Git-Integrationstest in `tests/Feature/Git/`: Nach einem unklaren Pushergebnis prüft der Retry zuerst den Remotezustand; bei bereits identischem Remoteref wird kein zweites Mal gepusht, und der Zustand gilt als bestätigt.
- **TC-16** Git-Integrationstest in `tests/Feature/Git/`: Remotedrift und ein nicht vorspulbarer Remotezustand werden je einzeln gefahren und blockieren mit verständlichem benanntem Grund; der Remoteref bleibt unverändert, und die Runbasis wird nicht rebased.
- **TC-17** Feature-Test in `tests/Feature/Runs/`: Ein ungültiges beziehungsweise veraltetes Security- oder Reviewresultat sowie ein offenes und ein stale gewordenes Gate werden je einzeln gefahren; in keinem Fall entsteht ein finaler Commit oder ein Push.
- **TC-18** Feature-Test in `tests/Feature/Runs/`: Nach bestätigtem Branch-Push werden Soft-, Block- und Hard-Cancel je einmal versucht und abgewiesen; ausschließlich die Statussynchronisation setzt den Run fort, und der Push wird nicht zurückgerollt.
- **TC-19** Git-Integrationstest in `tests/Feature/Git/`: Cleanup nach Erfolg und nach Fehler wird je zweimal ausgeführt und ist idempotent; Worktree und transiente Arbeitsdaten sind entfernt, der veröffentlichte Branch und die Auditdaten sind unverändert vorhanden.
- **TC-20** Feature-Test in `tests/Feature/HumanLoop/`: Manueller Push und Konfliktentscheidung verwenden die vorhandene Interventionsroute und `intervene_run`, verlangen Authentifizierung, projektbezogene Autorisierung, gültiges CSRF-Token und die erwartete Runversion; eine veraltete Runversion wird ohne Teilwirkung abgewiesen, und im Routen- sowie Autorisierungsinventar entsteht kein neuer Eintrag.
- **TC-21** Unit-Test in `tests/Unit/Runs/`: Nach dem Bootstrap trägt `manual_push` Producer sowie die Resolver autorisierte Pushaktion auf unverändertem Candidate und Cancel, und `status_sync` ist genau einmal mit einer Registrierung eingetragen, die report-only Abschluss und Post-Push-Synchronisation gemeinsam abdeckt; eine zweite abweichende Registrierung desselben Grundes lässt den Bootstrap fehlschlagen.

## AC Coverage

| AC | Evidence |
|---|---|
| AC-01 | TC-01 |
| AC-02 | TC-02 |
| AC-03 | TC-03 |
| AC-04 | TC-04 |
| AC-05 | TC-05 |
| AC-06 | TC-06 |
| AC-07 | TC-07 |
| AC-08 | TC-08 |
| AC-09 | TC-09 |
| AC-10 | TC-10 |
| AC-11 | TC-11 |
| AC-12 | TC-12 |
| AC-13 | TC-13 |
| AC-14 | TC-14 |
| AC-15 | TC-15 |
| AC-16 | TC-16 |
| AC-17 | TC-17 |
| AC-18 | TC-18 |
| AC-19 | TC-19 |
| AC-20 | TC-20 |
| AC-21 | TC-21 |

## Initial Scope and Sensitive Paths

**Expected initial scope:**

- `app/AI6/Git/` — existing
- `app/AI6/Runs/` — existing
- `app/AI6/Tickets/` — existing
- `app/AI6/HumanLoop/` — existing
- `app/AI6/Shared/AI6ServiceProvider.php` — existing
- `database/migrations/` — existing
- `tests/Unit/Git/` — existing
- `tests/Unit/Runs/` — existing
- `tests/Unit/Tickets/` — existing
- `tests/Feature/Git/` — existing
- `tests/Feature/Runs/` — existing
- `tests/Feature/HumanLoop/` — existing
- `tests/Unit/ScaffoldStructureTest.php` — existing
- `README.md` — existing

**Sensitive paths:**

- `database/migrations/` — Migrationskategorie; erwartete Entscheidung: genehmigt für genau eine Folgemigration, die Candidate-Commitbindung, Pushzustand, Post-Push-Sagabindung und Recorded-Scope-Nachweis anlegt. Veröffentlichte Migrationen bleiben unverändert.
- `app/AI6/Tickets/` — neuer Statusübergang `in_progress → review` innerhalb der geschlossenen Statusoperationsmenge; erwartete Entscheidung: genehmigt ausschließlich als sagaeigener Übergang ohne generischen Pfad und ohne Änderung veröffentlichter Rollenzuordnungen.
- `app/AI6/Git/` — finaler Commit, Push und Statuscommit auf dem verwalteten Repository; erwartete Entscheidung: genehmigt ohne zweite Refmutation, ohne Force-Push jenseits der vorhandenen Compare-and-Swap-Naht und ohne automatisches Merge.

## Do Not Change

- `app/AI6/Reviews/` — Reviewergebnisse, Findings und Dispositionen werden ausschließlich gelesen; dieses Ticket erzeugt kein Reviewergebnis.
- `app/AI6/Checks/` — Checkprofile und Checkergebnisse werden unverändert konsumiert.
- `app/AI6/Agents/` — Adaptervertrag, Sessions und Instruktionssnapshot bleiben unverändert; der Abschluss ruft keinen Provider auf.
- `app/AI6/Prompts/` — Katalog und Renderer bleiben unverändert.
- `app/AI6/Projects/` — Projektmetadaten, Konfigurationsbindung und Control-Branch-Vertrag bleiben unverändert.
- `app/AI6/Auth/` — Authentifizierung und Step-up bleiben unverändert; die Pushaktion nutzt die vorhandene Naht.
- `app/AI6/Shared/Redaction/` — die zentrale UTF-8- und Redaction-Grenze wird konsumiert und nicht dupliziert.
- `app/AI6/Shared/Process/` — Prozessgrenze, Mailbox und Effektsperre werden unverändert konsumiert.
- `app/AI6/Shared/Security/` — Securityprofil und Policyhash bleiben unverändert.
- `app/AI6/Shared/Http/ContentSecurityPolicy.php` — die feste CSP wird für die Anzeige nicht gelockert.
- `resources/views/` — die vorhandene generische Interventionsansicht wird unverändert verwendet; die vollständige Pushdarstellung gehört in `AI6-031`.
- `routes/web.php` — manueller Push und Konfliktentscheidung nutzen die vorhandene Human-Request-/Interventionsroute; es entsteht keine zweite Route.
- `config/ai6.php` — Pushmodi, Refmuster und Servermaxima sind bereits konfiguriert; dieses Ticket führt keinen neuen Konfigurationswert ein.
- `composer.json` — dieses Ticket führt keine neue Abhängigkeit ein.
- `composer.lock` — unverändert, weil keine Abhängigkeit hinzukommt.
- `docs/AI6_IMPLEMENTATION_PLAN.md` — normative Quelle.
- `AGENTS.md` — freigegebene Instruktionsquelle.
- `CLAUDE.md` — importiert ausschließlich `AGENTS.md`.
- `tickets/` — Ticketinhalt und Ticketstatus dieses Repositorys werden vom Implementierungsagenten nicht verändert; die Rückschreibung nach `TKT-012` betrifft ausschließlich Ticketdateien verwalteter Projekte zur Laufzeit.

## Out of Scope

- Erzeugung und Absicherung des Publish-Kandidaten sowie Finalchecks und Provenienz-Preflight (`AI6-027`).
- Der optionale LLM-Sicherheitsreview und sein Wartestatus (`AI6-028`); dieses Ticket liest ausschließlich dessen Ergebnis.
- Projektqueue, Eligibility und abhängigkeitssicherer Auto-Start nach Runabschluss (`AI6-030`).
- Retention, Tombstones und die vollständige mobile Runbeobachtung (`AI6-031`).
- Die vollständige FakeAgent-E2E- und Recoverymatrix (`AI6-032`).
- Automatisches Merge und automatische Pull-Request-Erzeugung.
- Push auf fremde Branches oder außerhalb des Run-Branch-Namensraums.

## Manual and External Gates

None.

## Review Focus

- Die Reihenfolge ist die Kontrolle: Ein Statuscommit vor bestätigtem Branchref verletzt `GIT-008` und macht den Ticketstatus zu einer Behauptung über einen Stand, den niemand sehen kann.
- Genau ein Parent, genau ein Tree: Ein finaler Commit, dessen Tree-OID nicht mit der geprüften Candidate-Tree-OID übereinstimmt, veröffentlicht ungeprüften Code. Der Nachweis muss am Objektstore hängen, nicht an einer mitgeführten Variablen.
- Der No-change-Zweig ist kein Sonderfall zum Abkürzen: Er muss dieselben Checks, Abdeckungen, Reviews und Gates durchlaufen haben und darf trotzdem keinen leeren Commit erzeugen.
- Idempotenz gegen Wiederholung, nicht gegen Gleichzeitigkeit: Ein Retry, der den Remotezustand nicht zuerst liest, ist der wahrscheinlichste Weg zu einem doppelten Push.
- Reconciliation nur des eigenen Commits: Ein fremder, ähnlich aussehender `review`-Commit ist der klassische Angriff auf eine Statussaga; die Bindung an den eigenen persistierten Commit ist die einzige zulässige Erkennung.
- Der Recorded-Scope-Block ist Dokumentation und darf sich nicht wie ein Vertrag verhalten: Jede Stelle, an der er in den Vertragshash eingeht, Reviewevidenz invalidiert oder als Contract Amendment behandelt wird, ist ein Befund.
- Genau ein Commit für Status und Scope: Zwei Commits für dieselbe Wirkung erzeugen einen Zwischenzustand, in dem der Ticketstatus bereits `review` ist, die Scope-Dokumentation aber fehlt.
- Registerkonflikt statt zweiter Registrierung: `status_sync` ist bereits eingetragen; eine zweite, abweichende Registrierung desselben Grundes lässt den Bootstrap fehlschlagen und ist der naheliegendste Umsetzungsfehler dieses Tickets.
- Sagaeigener Statusübergang: Ein über den generischen Statuspfad erreichbarer `in_progress → review` umgeht die gesamte Push- und Bindungsprüfung.
- Sperre bleibt Sperre: Eine Sperrfreigabe vor bestätigtem eigenem Statuscommit erlaubt einen zweiten Run auf demselben Projekt und ist ein schwerwiegender Befund.
- Cleanup darf nichts Veröffentlichtes entfernen: Das Abräumen von Worktree und Arbeitsdaten muss den veröffentlichten Branch und jede Auditzeile unberührt lassen.
- Kein Git im Browserrequest: Push und Konfliktentscheidung sind autorisierte Auslöser eines Workerauftrags, niemals eine direkte Git-Ausführung im Request.

## Notes

- Definition of Done: `docs/AI6_IMPLEMENTATION_PLAN.md` §12.2.
- Ticket status, approval and run metadata are owned by AI6. The implementation agent never changes them.
- Necessary deviations are reported as `needs_human` or as a scope/contract request, never applied silently.
- Rebase-Verpflichtung nach Plan §13.6 und Template §9.1: Vor `status: ready` werden `files`, die Scope-Marker sowie jede aus `AI6-027` und `AI6-028` stammende Klasse, Naht, Bindung und Testbezeichnung gegen den dann realen Repositoryzustand neu abgeleitet. `AI6-009` und `AI6-014` sind bereits integriert und wurden im Stand `cf4183a` verifiziert. Bis zum Rebase ist dieses Ticket nicht umsetzungsbereit und darf weder freigegeben noch beansprucht werden; der Status bleibt eine separate menschliche Entscheidung.
- Die `— existing`-Marker dieses Tickets bezeichnen die Runbasis nach dem Landen der `depends_on`-Tickets.
- Hilfreiche Planstellen in deutscher Prosa: Ablaufschritte 17 bis 20 in Plan §8, adaptive Scope-Regel und Rückschreibung Plan §8.2, Statuswerte und Übergänge Plan §5.2, Wartestatusregister, Präzedenz und Abbruchverbot nach bestätigtem Push Plan §7.2, manuelle und externe Gates Plan §8.5.
- Ein Teil der Git- und Prozessnachweise beweist POSIX-Semantik und überspringt sich außerhalb der Linux-Laufzeit selbst; ein grüner Windowslauf ist für diese Fälle keine vollständige Evidenz.
