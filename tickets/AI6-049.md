---
schema: ai6.ticket.v1
id: AI6-049
title: "Backup, Restore, Rotation und Disaster Recovery"
status: todo
depends_on: [AI6-002, AI6-005A, AI6-029, AI6-031]
kind: chore
milestone: M7
risk: high
files:
  - "app/AI6/Shared/Operations/BackupCommand.php"
  - "app/AI6/Shared/Operations/RestoreCommand.php"
  - "app/AI6/Shared/Operations/BackupSet.php"
  - "app/AI6/Shared/Operations/BackupManifest.php"
  - "app/AI6/Shared/AI6ServiceProvider.php"
  - ".env.example"
  - "docker-compose.yml"
  - "README.md"
  - "docs/AI6-049_MG-01_ABNAHMEPROTOKOLL.md"
  - "tests/Feature/Shared/Operations/"
  - "tests/Unit/Shared/Runtime/RuntimeScriptsTest.php"
  - "tests/Unit/ScaffoldStructureTest.php"
spec_refs:
  - "docs/AI6_IMPLEMENTATION_PLAN.md — SEC-010"
  - "docs/AI6_IMPLEMENTATION_PLAN.md — SEC-011"
---

# AI6-049 — Backup, Restore, Rotation und Disaster Recovery

## Goal

Eine ruhende AI6-Instanz auf demselben Software- und Schemastand sichern und wiederherstellen, ohne dass Schlüssel im Backup liegen, Sessions weiterleben oder abgelaufene Rohdaten zurückkehren.

Nach diesem Ticket sichert ein Betreiber die Instanz in einem dokumentierten Wartungsfenster mit `ai6:backup` und stellt sie mit `ai6:restore` wieder her; das Backup ist ein Verzeichnis mit Manifest, der Restore prüft alles vor der ersten Änderung, widerruft Sessions und setzt die Retention idempotent fort. Ein Reviewer erkennt den Erfolg daran, dass ein nicht ruhender Zustand, ein manipuliertes Backup oder ein abweichender Stand benannt abgewiesen wird, ein unvollständiger Restore sichtbar unvollständig bleibt, und die Backup-, Restore-, Rotations- und Retentionstests grün sind.

## Context

Der Repositorystand `a7d83e9` enthält alle vier Abhängigkeiten als integrierten Code mit `status: done`. Dieses Ticket entstand mit Planrevision V1.7.9 als Split aus `AI6-036`; Installation, Zugang und Doctor bleiben dort, und beide Tickets sind unabhängig voneinander umsetzbar. Persistent sind die benannten Compose-Volumes `ai6_database` (`/var/lib/ai6/database/database.sqlite`), `ai6_storage` (`/opt/ai6/storage`), `ai6_executions`, `ai6_managed` (Managed-Clones, `deploy-keys/` mit den privaten Deploy-Keys, `known_hosts` unter `AI6_CONTROL_OPERATION_KNOWN_HOSTS_FILE`) sowie der Provider-Store `ai6_provider_store` der Agentrolle, den der Worker nicht liest. Die Compose-Datei reicht jeder Rolle nur eine feste Umgebungsliste weiter: `APP_KEY` erhalten `app` und `worker`, `APP_PREVIOUS_KEYS` wird keiner Rolle übergeben; eine Rotation im Container ist deshalb erst nach einer minimalen Compose-Ergänzung möglich. Laravel löst `APP_PREVIOUS_KEYS` in `previous_keys` auf und entschlüsselt damit nach einer Rotation weiterhin alte Werte.

Verschlüsselte Daten in AI6 sind ausschließlich die mit dem Laravel-`Encrypter` gebundenen TOTP-Geheimnisse (`TotpSecretCipher::decrypt()`); Runartefakte werden redigiert, nicht verschlüsselt. Sessions liegen in `sessions` (`UserSession`), Login-Code-Challenges in `login_confirmations`, WebAuthn- und Enrollment-Challenges in der Session; `tests/Feature/Auth/SessionRevocationTest.php` zeigt, wie ein Client nach entfernter Session ausgeloggt wird, und Recovery-Codes werden über `ai6:reissue-recovery-codes` (`ReissueRecoveryCodesCommand`) neu ausgegeben. Der Redaction-Schlüsselring (`RedactionKeyring` mit `has()`, `keyOf()`, `versionOf()`, `keyIds()`, `usesApplicationKeyFallback()`) trägt Key-ID und Version jedes Fingerprints; `RunRetentionSweepTest` belegt, dass Tombstones unter ihrer eigenen Key-ID verglichen und unter einem nicht mehr vorhandenen Schlüssel namentlich verweigert werden. `RunArtifactStore::bytes()` gibt ab `expires_at` keine Bytes mehr heraus, `purge()` wird ausschließlich vom `RunRetentionSweep` aufgerufen und schreibt den gebundenen Tombstone; die SQLite-Guards aus `database/migrations/2026_09_02_000000_add_run_artifact_retention_contract.php` lassen keine Rückkehr in `stored` zu; jede Löschung folgt aus der persistierten Ablaufzeit. `RunRetentionSweep::sweep()` behandelt `run_artifacts`, `run_events` und `check_results` und liefert ein `RunRetentionSweepResult` mit `deferred` und `failed`; die Downloadroute antwortet auf abgelaufene oder gelöschte Artefakte mit 410. `ProjectOperationLease::claimInitialDeployKeyProvisioning()` akzeptiert nur `NOT_PROVISIONED` und `PROVISIONING_FAILED`; ein bereits provisioniertes Projekt kann seinen Deploy-Key nicht neu provisionieren, weshalb die privaten Deploy-Keys Teil des Backups sind. Managed-Clones sind über die vorhandene Clone-Operation aus dem Remote rekonstruierbar.

Neue Artefakte dieses Tickets, die im Repository noch nicht existieren: die Kommandos `ai6:backup` und `ai6:restore` mit `BackupSet` und `BackupManifest` im neuen Verzeichnis `app/AI6/Shared/Operations/` (gehört allein diesem Ticket; `AI6-036` legt seinen Installationsassistenten neben dem Doctor ab und wird zuerst integriert, weil das Restore-Gate eine nach `README.md` installierte Instanz voraussetzt), das Testverzeichnis `tests/Feature/Shared/Operations/` und das Protokoll `docs/AI6-049_MG-01_ABNAHMEPROTOKOLL.md`. Ein Backup ist ein gewöhnliches Verzeichnis mit Manifest, kein Archivformat, und der unterstützte Betriebsfall ist ausdrücklich begrenzt: Sicherung und Wiederherstellung einer ruhenden Instanz auf demselben Software- und Schemastand. Es entsteht keine zweite Retention-, Redaction- oder Recoverylogik.

## Tasks

1. Das neue Kommando `ai6:backup {target?}` mit `BackupSet` und `BackupManifest` anlegen und im `AI6ServiceProvider` registrieren. Betriebsfall: Alle dauerhaften Rollen sind gestoppt, das Kommando läuft in einem einmaligen Container mit Workeridentität. Vor jeder Kopie prüft es den ruhenden Zustand und weist ein Run in nicht terminalem Zustand oder eine ausstehende beziehungsweise beanspruchte Control Operation als `backup_state_not_quiescent` ab. Inhalt: ein SQLite-Snapshot über `VACUUM INTO` (andere Treiber werden benannt abgewiesen), der Artefaktbaum aus `RunArtifactRoot` unter `run-artifacts/`, das Verzeichnis `deploy-keys/` und die `known_hosts`-Datei des Workers unter `managed/` mit erhaltenen Dateimodi; kopiert werden ausschließlich reguläre Dateien über `lstat`, ein Symlink oder Sonderdatei bricht mit `backup_unsupported_entry` ab. Zielverzeichnis standardmäßig `storage/app/ai6/backups/<UTC-Zeitstempel>/` mit Modus `0700`; ein vorhandenes Ziel oder ein Ziel innerhalb eines Quellbaums wird abgewiesen. `manifest.json` (Schema `ai6.backup.v1`) wird als letzte Datei geschrieben und trägt Erstellungszeit, `APP_ENV`, die Namen der angewendeten Migrationen aus `migrations`, je Nutzdatei relativen Pfad, SHA-256 und Größe sowie je in `run_artifacts`, `run_events` und `check_results` vorkommender `fingerprint_key_id` deren Version und einen Prüf-HMAC einer festen Zeichenkette unter `keyOf()`. Das Backup enthält weder `.env`, `APP_KEY`, `APP_PREVIOUS_KEYS`, Schlüsselringwerte noch Provider-Store-Inhalte; ein Verzeichnis ohne vollständiges Manifest ist kein Backup.
2. Das neue Kommando `ai6:restore {source}` anlegen, das im selben Betriebsfall läuft und vor der ersten Änderung vollständig prüft: Manifestschema; ausschließlich relative Pfade ohne `..`, ohne Duplikate und ohne Symlinks; SHA-256 und Größe jeder Datei; `PRAGMA integrity_check` des Snapshots; Gleichheit der Migrationsnamen des Snapshots mit den Migrationsdateien des laufenden Codes (`restore_schema_mismatch`); Entschlüsselbarkeit jedes im Snapshot enthaltenen TOTP-Geheimnisses mit dem aktuellen `APP_KEY`/`APP_PREVIOUS_KEYS` ohne Ausgabe des Klartexts (`restore_totp_undecryptable`); Vorhandensein jeder `fingerprint_key_id` im Schlüsselring mit gleicher Version und gleichem Prüf-HMAC (`restore_redaction_key_missing`, `restore_redaction_key_mismatch`). Jeder Befund endet benannt mit Exitcode 1, ohne dass Datenbank, Artefaktbaum, `managed/` oder Tabellen verändert wurden. Erst danach folgt die Umschaltung: Datenbankverbindung trennen, aktuelle Datenbankdatei samt `-wal`/`-shm`, Artefaktbaum, `deploy-keys/` und `known_hosts` nach `<pfad>.pre-restore-<UTC-Zeitstempel>` verschieben, geprüfte Daten hineinkopieren, Verbindung neu aufbauen, alle Zeilen in `sessions` und `login_confirmations` löschen, `RunRetentionSweep::sweep()` genau einmal ausführen. Endet der Sweep mit `failed` oder `deferred` ungleich null, meldet das Kommando „Restore unvollständig“ und endet ungleich null; bei einem Fehler während der Umschaltung nennt es je Pfad, ob er den alten oder den neuen Stand trägt, und lässt die `.pre-restore`-Kopien stehen. Nur bei vollständigem Erfolg werden die `.pre-restore`-Kopien entfernt und die Sweep-Zähler ausgegeben. Es gibt keine allgemeine Atomaritätszusage über mehrere Bäume; die Dokumentation beschreibt den Rückweg über die stehengelassenen Kopien.
3. Deploy- und Umgebungsdateien minimal ergänzen: `docker-compose.yml` erhält in genau den beiden Rollen mit `APP_KEY` (`app`, `worker`) die Weitergabe `APP_PREVIOUS_KEYS`; `.env.example` erhält die auskommentierte Zeile `APP_PREVIOUS_KEYS=` mit deutschem Hinweis. Alle anderen Rollen, Mounts und Werte bleiben unverändert.
4. `README.md` um den Abschnitt „Backup, Restore, Rotation und Disaster Recovery“ ergänzen: Wartungsfenster mit gestoppten dauerhaften Rollen und dem einmaligen Container (`docker compose run --rm --entrypoint php worker artisan ai6:backup` beziehungsweise `ai6:restore`); die Wiederherstellungstabelle (im Backup: Datenbank, Artefakte, Deploy-Keys, `known_hosts`; getrennt zu sichern: `.env` als Schlüsselpaket mit `APP_KEY`, `APP_PREVIOUS_KEYS` und Schlüsselring; nach Restore neu einzurichten: Providerlogins nach `AI6-035`; aus dem Remote rekonstruierbar: Managed-Clones über die vorhandene Clone-Operation); Schutz und Aufbewahrung der Backupkopien durch den Betreiber (Rechte, Off-Host-Kopie, einfache Aufbewahrungsregel, Backupaufbewahrung ist nicht die Live-Retention); die Wiederanlaufprüfung nach Restore (Gitstand des Control-Branch und offene Wirkungen prüfen, wiederhergestellte Zugangsrechte und Recovery-Codes prüfen und nötigenfalls mit `ai6:reissue-recovery-codes` neu ausgeben, erst dann Rollen starten); `APP_KEY`-Rotation über `APP_PREVIOUS_KEYS` mit einfacher Aufbewahrungsregel für alte Schlüssel; Schlüsselring-Rotation; die Zusicherung, dass abgelaufene Rohdaten nach einem Restore nicht zurückkehren; Restore nur auf demselben Stand, Upgrade danach als getrennter Schritt.
5. `docs/AI6-049_MG-01_ABNAHMEPROTOKOLL.md` als ergebnisfreie Vorlage anlegen: Bindung an Commit, Image-Digest und Host; `ai6:backup` im Wartungsfenster; `ai6:restore` auf einer frischen Instanz ohne die ursprünglichen Volumes mit beobachtetem Sessionwiderruf, fortgesetzter Retention, funktionierendem Git-Zugriff eines wiederhergestellten Projekts und anschließender `APP_KEY`-Rotation; weder Implementierung noch Tests tragen ein Ergebnis ein.
6. Tests anlegen: `tests/Feature/Shared/Operations/` für Backup, Restore, unvollständige Umschaltung, Rotation und Retentionszenarien; die Compose-Weitergabe in `tests/Unit/Shared/Runtime/RuntimeScriptsTest.php`; die neuen Dateien und Dokumentationsaussagen in `tests/Unit/ScaffoldStructureTest.php`.

## Acceptance Criteria

- [ ] **AC-01** `ai6:backup` weist eine Instanz mit nicht terminalem Run oder ausstehender Control Operation als `backup_state_not_quiescent` ab und erzeugt sonst ein Verzeichnis mit `VACUUM INTO`-Snapshot, Artefaktbytes, `deploy-keys/`, `known_hosts` und zuletzt geschriebenem `manifest.json` mit Migrationsnamen, relativen Pfaden, SHA-256, Größen sowie Version und Prüf-HMAC je Key-ID; Symlinks, vorhandene oder verschachtelte Ziele werden benannt abgewiesen, und keine Backupdatei enthält `APP_KEY`, `APP_PREVIOUS_KEYS`, Schlüsselringwerte oder Provider-Store-Inhalte.
- [ ] **AC-02** `ai6:restore` weist ein Backup mit verfälschter Datei, Pfadausbruch, Duplikat, beschädigtem Snapshot, abweichendem Migrationsstand, nicht entschlüsselbarem TOTP-Geheimnis oder einer im Schlüsselring fehlenden beziehungsweise mit anderem Material konfigurierten Key-ID benannt ab, ohne Datenbank, Artefakte, `managed/`, Sessions oder Challenges zu verändern; nach einem erfolgreichen Restore sind die TOTP-Geheimnisse entschlüsselbar, `sessions` und `login_confirmations` leer, ein zuvor angemeldeter Client ist beim nächsten Request ausgeloggt, und die `.pre-restore`-Kopien sind entfernt.
- [ ] **AC-03** Ein Fehler während der Umschaltung oder ein Sweep mit `failed` oder `deferred` ungleich null lässt `ai6:restore` mit „Restore unvollständig“ ungleich null enden, die `.pre-restore`-Kopien bleiben stehen, und die Ausgabe nennt je Pfad den tatsächlichen Stand.
- [ ] **AC-04** Nach einem Restore bleibt eine Rotation möglich: Ein neuer `APP_KEY` mit dem alten Wert in `APP_PREVIOUS_KEYS` entschlüsselt weiterhin die wiederhergestellten TOTP-Geheimnisse, ein nach einer Schlüsselring-Rotation neu gespeichertes Artefakt trägt die neue Key-ID, und ein wiederhergestellter Tombstone bleibt an seine alte Key-ID gebunden und verweigert die Wiederablage seines Inhalts; `docker-compose.yml` reicht `APP_PREVIOUS_KEYS` genau an `app` und `worker` weiter.
- [ ] **AC-05** Ein Backup mit gültigem Artefakt, einem bis zum Restore ablaufenden Artefakt, einem bis zum Restore ablaufenden Rohlog in `run_events` und einem Tombstone führt nach dem Restore dazu, dass die abgelaufenen Rohdaten weder in der Runansicht noch über die Downloadroute (410) noch aus `RunArtifactStore::bytes()` lesbar sind, der Restore-Sweep sie tombstoned und die Storagebytes entfernt, das gültige Artefakt lesbar bleibt, der Tombstone ein Tombstone bleibt und ein erneuter Sweep nichts mehr verändert; ein nach dem Backup regulär abgelaufenes und gelöschtes Artefakt kehrt nach dem Restore nicht zurück.
- [ ] **AC-06** `README.md` dokumentiert Wartungsfenster, Wiederherstellungstabelle, Backupschutz, Wiederanlaufprüfung, Rotation und Disaster Recovery, `.env.example` führt `APP_PREVIOUS_KEYS`, und eine Restore-Übung auf einer frischen Instanz ohne die ursprünglichen Volumes folgt diesem Pfad nachweislich.

## Test Cases

- **TC-01** Feature-Test Backupinhalt: Nach gespeicherten Artefakten, einem TOTP-Geheimnis, Sessions und einem Deploy-Key-Verzeichnis enthält das Backupverzeichnis Snapshot, Artefaktbytes, `managed/deploy-keys/`, `known_hosts` und das zuletzt geschriebene Manifest mit passenden SHA-256-Werten, Migrationsnamen und Key-ID-Einträgen; der Snapshot ist als eigenständige SQLite-Datei lesbar; ein nicht terminaler Run, eine offene Control Operation, ein Symlink im Artefaktbaum, ein vorhandenes Ziel und ein Ziel innerhalb des Artefaktbaums führen je zu Exitcode 1 mit benanntem Grund ohne geschriebene Datei; in keiner Backupdatei kommen `APP_KEY`, Schlüsselringbytes oder `.env`-Inhalt vor.
- **TC-02** Feature-Matrix der Restore-Abweisung: Verfälschte Artefaktdatei, Manifestpfad mit `..`, absoluter Pfad, doppelter Pfad, Symlink im Backup, beschädigter Snapshot mit neu berechneter Prüfsumme, Snapshot mit fehlender Migration, unter anderem `APP_KEY` verschlüsseltes TOTP-Geheimnis, fehlende `fingerprint_key_id` sowie gleiche Key-ID mit anderen Schlüsselbytes oder anderer Version führen je zu Exitcode 1 mit benanntem Grund, und Datenbankdatei, Artefaktbaum, `managed/`, `sessions` und `login_confirmations` sind danach byte- beziehungsweise zeilenidentisch mit dem Zustand davor.
- **TC-03** Feature-Test erfolgreicher Restore: Nach dem Restore eines gültigen Backups entschlüsselt `TotpSecretCipher` das wiederhergestellte Geheimnis, `sessions` und `login_confirmations` sind leer, ein vor dem Restore angemeldeter Client erhält beim nächsten Request die Anmeldeseite, die wiederhergestellten Artefakte sind über `RunArtifactStore::bytes()` lesbar, die Deploy-Key-Datei liegt mit Modus `0600` an ihrem Pfad, und keine `.pre-restore`-Kopie ist übrig.
- **TC-04** Feature-Test unvollständige Umschaltung: Ein injizierter Fehler beim Artefakttausch und ein injizierter Löschfehler im Restore-Sweep führen je zu „Restore unvollständig“ mit Exitcode 1, die `.pre-restore`-Kopien bleiben erhalten, und die Ausgabe nennt je Pfad den tatsächlichen Stand; ein Test ausschließlich mit vorab beschädigten Backupdateien genügt nicht.
- **TC-05** Feature-Test Backup/Restore mit anschließender Rotation: Nach dem Restore wird `APP_KEY` mit dem alten Wert in `APP_PREVIOUS_KEYS` gewechselt und das TOTP-Geheimnis bleibt entschlüsselbar; nach einer Schlüsselring-Rotation trägt ein neu gespeichertes Artefakt die neue Key-ID, der wiederhergestellte Tombstone behält seine alte Key-ID und Version, und die erneute Ablage seines Inhalts wird weiterhin verweigert; die Compose-Prüfung belegt `APP_PREVIOUS_KEYS` genau in `app` und `worker`.
- **TC-06** Feature-Test mit Uhrzeitsteuerung vor und nach Retentionablauf: Ein Backup mit gültigem Artefakt, später ablaufendem Artefakt, später ablaufendem Rohlog in `run_events` und Tombstone wird nach dem Ablauf auf terminalen Runs wiederhergestellt; die abgelaufenen Rohdaten liefern keine Bytes, die Downloadroute antwortet 410, der Restore-Sweep tombstoned sie und entfernt die Storageobjekte, das gültige Artefakt bleibt lesbar, der Tombstone bleibt unverändert, ein zweiter Sweep meldet null Änderungen; zusätzlich kehrt ein nach dem Backup abgelaufenes und gelöschtes Artefakt nach dem Restore nicht zurück.
- **TC-07** Dokumentations- und Inventartests: `README.md` enthält den Abschnitt mit Wartungsfenster, Wiederherstellungstabelle, Backupschutz, Wiederanlaufprüfung, Rotation und Disaster Recovery; `.env.example` enthält `APP_PREVIOUS_KEYS=`; die Scaffoldinventur führt alle neuen Dateien.

## AC Coverage

| AC | Evidence |
|---|---|
| AC-01 | TC-01 |
| AC-02 | TC-02, TC-03, MG-01 |
| AC-03 | TC-04 |
| AC-04 | TC-05, MG-01 |
| AC-05 | TC-06 |
| AC-06 | TC-07, MG-01 |

## Initial Scope and Sensitive Paths

**Expected initial scope:**

- `app/AI6/Shared/Operations/BackupCommand.php` — new
- `app/AI6/Shared/Operations/RestoreCommand.php` — new
- `app/AI6/Shared/Operations/BackupSet.php` — new
- `app/AI6/Shared/Operations/BackupManifest.php` — new
- `app/AI6/Shared/AI6ServiceProvider.php` — existing
- `.env.example` — existing
- `docker-compose.yml` — existing
- `README.md` — existing
- `docs/AI6-049_MG-01_ABNAHMEPROTOKOLL.md` — new
- `tests/Feature/Shared/Operations/` — new
- `tests/Unit/Shared/Runtime/RuntimeScriptsTest.php` — existing
- `tests/Unit/ScaffoldStructureTest.php` — existing

**Sensitive paths:**

- `docker-compose.yml` — Deploy-Pfad: Weitergabe von `APP_PREVIOUS_KEYS` an genau `app` und `worker`. Erwartete Entscheidung: Freigabe genau dieser Zeilen; Rollen, Mounts, Netze und alle anderen Werte bleiben unverändert, und keine Rolle erhält Credentials einer anderen Rolle.

## Do Not Change

- `Dockerfile`, `docker/entrypoint.sh` — Image-, Rollen- und Migrationsvertrag bleiben unverändert; Backup und Restore verwenden die vorhandenen Volumes und einen einmaligen Container mit bestehender Rollenidentität.
- `database/migrations/` — kein Schemawechsel; ein Restore ersetzt die Datenbankdatei auf demselben Migrationsstand und führt keine Migration aus.
- `app/AI6/Runs/RunRetentionSweep.php`, `app/AI6/Runs/RunArtifactStore.php` — die Ablaufsperre und der Tombstone-Vertrag werden vom Restore konsumiert, nicht verändert.
- `app/AI6/Shared/Security/`, `app/AI6/Shared/Redaction/` — Policy- und Schlüsselringsemantik bleiben unverändert; der Restore liest den Ring nur.
- `app/AI6/Git/` — Deploy-Key-Provisionierung, Managed-Clone und Hostkeyprüfung bleiben unverändert; das Backup kopiert deren Dateien, es erzeugt keinen zweiten Recoverypfad.
- `app/AI6/Auth/` — Sessionwiderruf erfolgt ausschließlich über die vorhandenen Modelle `UserSession` und `LoginConfirmation`, Recovery-Codes über das vorhandene Kommando; Authentifizierungslogik wird nicht angefasst.

## Out of Scope

- Online-Sicherung bei laufenden Rollen, Sicherung aktiver Runs, Restore auf einen anderen Software- oder Schemastand, automatischer Backup-Transport, Backup-Scheduling und Verschlüsselung des Backupverzeichnisses; der Betreiber sichert Verzeichnis und `.env` mit eigenen Mitteln außerhalb des Repositorys.
- Backup des Provider-Stores; nach einem Restore werden Providerlogins (`AI6-035`) neu durchgeführt.
- Ein Recoverypfad für bereits provisionierte Projekte ohne Deploy-Key; die Deploy-Keys sind Teil des Backups.
- Installation, Zugang, Doctor und Release-Gate (`AI6-036`); der reale Pilot (`AI6-038`).

## Manual and External Gates

- **MG-01** Menschliche Restore-Übung: `ai6:backup` im Wartungsfenster einer strict betriebenen Instanz, `ai6:restore` auf einer frischen Instanz ohne die ursprünglichen Volumen und ohne weiter vorhandene Schlüsseldateien mit beobachtetem Sessionwiderruf, fortgesetzter Retention, funktionierendem Git-Zugriff eines wiederhergestellten Projekts und anschließender `APP_KEY`-Rotation. Evidenz: ausgefülltes und signiertes `docs/AI6-049_MG-01_ABNAHMEPROTOKOLL.md` mit Commit, Image-Digest, Host, Datum und Prüfperson; automatisierte Tests und Windows-Läufe tragen kein Ergebnis ein.

## Review Focus

- Kein Backup- oder Restore-Ausgabepfad gibt Secret-, Schlüssel- oder Rohkonfigurationswerte aus; das Manifest trägt Hashes, Größen, Migrationsnamen, Key-IDs mit Version und Prüf-HMAC.
- Backup und Restore prüfen den ruhenden Zustand beziehungsweise alle Eingaben vollständig vor der ersten Änderung; Pfadausbrüche, Symlinks, Duplikate und Quell-/Zielüberlappung werden abgewiesen, und Prüfsummen sind Integritäts-, keine Authentizitätsnachweise.
- Die Umschaltung behauptet keine Atomarität über mehrere Bäume: Fehlerzustände sind benannt, die `.pre-restore`-Kopien bleiben erhalten, und ein unvollständiger Sweep lässt den Restore sichtbar unvollständig.
- Kein Restore-Pfad macht abgelaufene oder gelöschte Rohdaten wieder lesbar; die persistierte Ablaufzeit und der eine Sweep bleiben der einzige Prädikatspfad.
- Keine zweite Retention-, Redaction- oder Recoverylogik; kein Archivformat.
- Vollständige Scaffold-, Compose- und README-Inventuren.

## Notes

- Definition of Done: `docs/AI6_IMPLEMENTATION_PLAN.md` §12.2.
- Ticket status, approval and run metadata are owned by AI6. The implementation agent never changes them.
- Necessary deviations are reported as `needs_human` or as a scope/contract request, never applied silently.
- Abgeleitet am 19. September 2026 gegen den integrierten Stand `a7d83e9` (`AI6-035`) als Split aus `AI6-036` nach Planrevision V1.7.9 und dem Review `docs/AI6_M7_TICKET_REVIEW.md`; alle `depends_on`-Tickets sind im Code vorhanden.
- Das Verzeichnis `app/AI6/Shared/Operations/` und `tests/Feature/Shared/Operations/` werden von `AI6-036` und diesem Ticket gemeinsam genutzt; das zuerst integrierte Ticket legt sie an, das zweite ergänzt sie. Das ist keine gemeinsame instabile Naht, sondern ein gemeinsames Verzeichnis.
