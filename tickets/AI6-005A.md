---
schema: ai6.ticket.v1
id: AI6-005A
title: "Starke Primärauthentifizierung und E-Mail-Barriere"
status: todo
depends_on: [AI6-003, AI6-004]
kind: feature
milestone: M0
risk: high
files:
  - "composer.json"
  - "composer.lock"
  - "Dockerfile"
  - "database/migrations/"
  - "app/AI6/Auth/"
  - "app/AI6/Shared/AI6ServiceProvider.php"
  - "config/ai6.php"
  - "config/auth.php"
  - "config/mail.php"
  - "bootstrap/app.php"
  - "routes/web.php"
  - "resources/views/auth/"
  - "resources/views/mail/"
  - "public/"
  - ".env.example"
  - "tests/Unit/Auth/"
  - "tests/Feature/Auth/"
  - "README.md"
spec_refs:
  - "docs/AI6_IMPLEMENTATION_PLAN.md — SEC-002"
  - "docs/AI6_IMPLEMENTATION_PLAN.md — SEC-003"
  - "docs/AI6_IMPLEMENTATION_PLAN.md — HUM-003"
---

# AI6-005A — Starke Primärauthentifizierung und E-Mail-Barriere

## Goal

Jede neue Websession im strict-Profil erst nach starker Primärauthentifizierung und zusätzlicher E-Mail-Codebestätigung autorisieren.

Nach diesem Ticket bleibt eine Session nach erfolgreicher Primärauthentifizierung im Vorautorisierungszustand, bis derselbe Browser den achtstelligen Code eingegeben hat, der ausschließlich an die aus vertrauenswürdiger Konfiguration aufgelöste Bestätigungsadresse gesendet wurde. Kein Link in der E-Mail erzeugt eine Wirkung, ein Mailausfall verhindert die Autorisierung, und eine als kritisch markierte Aktion verlangt eine frische Wiederholung der Primärauthentifizierung.

## Context

Der Ausgangszustand ist das Ergebnis von `AI6-003` und `AI6-004`: die typisierte Instanzkonfiguration mit aufgelöster `SecurityPolicy`, dem Schalter für die Login-E-Mail-Bestätigung, dem Schalter für die Step-up-Pflicht, dem zentralen `Redactor` und `ai6:doctor` sowie Benutzer, globale Administratorrolle, Projektrollen, Basislogin, Datenbank-Sessionablage und Loginbegrenzung. Zur Erzeugungszeit dieses Tickets existiert keiner dieser Pfade; sie sind im Ausgangsscope als bestehend geführt und je Abhängigkeit zugeordnet: `AI6-003` erzeugt `config/ai6.php` mit den Sicherheitsschaltern, `app/AI6/Shared/AI6ServiceProvider.php` und den zentralen `Redactor`; `AI6-004` erzeugt `app/AI6/Auth/`, `resources/views/auth/`, `tests/Unit/Auth/`, `tests/Feature/Auth/`, die Datenbank-Sessionablage und den Auth-Konfigurationsvertrag. `composer.json`, `composer.lock`, `Dockerfile`, `config/auth.php`, `config/mail.php`, `bootstrap/app.php`, `routes/web.php`, `public/` und `.env.example` stammen aus `AI6-001` beziehungsweise `AI6-002` und werden über die Abhängigkeitskette weitergereicht.

Plan §10.3 beschreibt den Ablauf: Nach erfolgreicher Primärauthentifizierung bleibt die Session im Pre-Auth-Zustand, ein achtstelliger, kurzlebiger, einmaliger Code geht an die Adresse aus `AI6_LOGIN_CONFIRMATION_EMAIL` und wird im selben Browser eingegeben; kein Link löst eine Freigabe aus, und Mailausfall bleibt fail closed. Plan §8.3 trennt diese Adresse ausdrücklich von den späteren Human-Request-Mails, die an die verifizierte Adresse des verantwortlichen Benutzers gehen und nicht Teil dieses Tickets sind.

`SEC-002` verlangt für privilegierte Rollen zusätzlich ein Step-up. Der zugehörige Schalter entsteht bereits in `AI6-003`; die wirksame Wiederholungsprüfung entsteht hier, weil `AI6-006C` die Recoveryentscheidung und `AI6-006E` den auditierten Control-Branch-Wechsel darauf aufsetzen und beide dieses Ticket ausdrücklich als `depends_on` führen.

Die Cookie-, Host-, Proxy-, CSP- und Markdown-Härtung ist seit Planrevision V1.6.2 ein eigenes Ticket `AI6-005B`. Dieses Ticket liefert ausschließlich den Authentifizierungspfad und darf ihn nicht als vollständige Webhärtung ausgeben. Beide Tickets sind parallel **entwickelbar**, aber nicht parallel integrierbar, weil sie sechs Dateien gemeinsam berühren: `composer.json`, `composer.lock`, `app/AI6/Shared/AI6ServiceProvider.php`, `config/ai6.php`, `bootstrap/app.php` und `README.md`. Seit Planrevision V1.6.17 führt `AI6-005B` deshalb `AI6-005A` in `depends_on`; die Reihenfolge ist damit strukturell erzwungen statt nur zugesagt, und dieses Ticket löst `composer.lock` gegen den Base-Snapshot auf, während `AI6-005B` gegen den hier hinterlassenen Lockstand auflöst. Dieses Ticket berührt in den sechs gemeinsamen Dateien ausschließlich seinen eigenen Abschnitt — den Auth-Abschnitt in `config/ai6.php`, die eigenen Bindungen in `AI6ServiceProvider` und `bootstrap/app.php` sowie den eigenen `README.md`-Abschnitt.

Plan §10.3 und `SEC-002` beschreiben Anmeldung, Bestätigung und Step-up, aber nicht, wie ein Benutzer im strict-Profil überhaupt zu seinem ersten Passkey oder TOTP-Geheimnis kommt. Ohne eine Antwort sperrt genau dieses Ticket jeden Erstbenutzer aus: Die privilegierte Passkey-Pflicht greift, bevor ein Faktor existiert. Dieses Ticket schließt die Lücke als abgeleitete Entscheidung mit dem engstmöglichen Zuschnitt — Aufgabe 7, `AC-17` und `TC-19`: Ein Benutzer ohne registriertes starkes Verfahren erreicht nach korrektem Passwort ausschließlich die Registrierungsrouten für Passkey und TOTP und erhält keine autorisierte Session. Als starkes Verfahren zählen ausschließlich diese beiden; Recovery-Codes sind ein Wiederherstellungsmittel und beenden den Enrollment-Zustand nicht, weil ein Benutzer sonst ohne Primärverfahren aus ihm herausträte und anschließend ausgesperrt wäre. Es ist bewusst **genau ein** Weg. Ein zusätzlicher, administrativ ausgestellter Enrollment-Nachweis stand bis zur Berichtigung dieses Tickets gleichrangig daneben, ohne dass Autorisierung, Ablauf, Widerruf, Auditierung und Step-up-Verhältnis für ihn getrennt bestimmt waren; er ist entfallen und steht unter `## Out of Scope`. Er wird für den Erstzugang auch nicht gebraucht: Ein von einem globalen Administrator angelegter Benutzer besitzt ein Passwort und kommt damit selbst in den Enrollment-Zustand, und der erste globale Administrator entsteht über die Einrichtung aus `AI6-004`. Der Enrollment-Zustand ist kein zweiter Loginpfad: Er trägt keine Anwendungsberechtigung, endet mit der Registrierung, und die reguläre Anmeldung samt E-Mail-Barriere gilt danach unverändert. Der erste globale Administrator entsteht dabei nicht über diesen Weg, sondern über die Einrichtung aus `AI6-004`. Die Entscheidung steht hier, damit die Freigabe sie bewusst mitträgt; sie ist keine Erweiterung des Blueprintziels und keine Vorwegnahme einer Selbstregistrierung.

Passkeys und TOTP benötigen serverseitige Kryptofähigkeiten, die dieses Ticket nicht selbst implementiert. Welche davon der reale Abhängigkeitsbaum zum Umsetzungszeitpunkt bereits mitbringt, ist zur Erzeugungszeit nicht feststellbar, weil `composer.lock` noch nicht existiert. Die Auswahl wird deshalb erst gegen den dann realen Lockstand getroffen, folgt dabei aber einer hier festgelegten, verbindlichen Entscheidungsregel statt dem Ermessen des Umsetzenden: Aufgabe 1 und `AC-16` legen Reihenfolge, kumulative Eignungskriterien, Tie-Breaker und den fail-closed-Ausgang fest. Offen bleibt damit ausschließlich, welcher konkrete Kandidat diese Kriterien im realen Lockstand erfüllt — nicht, wonach entschieden wird. Erfüllt keiner sie, entsteht keine Bibliothek und keine eigene Kryptoimplementierung, sondern eine Entscheidungsanfrage.

## Tasks

1. Die WebAuthn- und TOTP-Fähigkeit gegen den dann realen Stand von `composer.json` und `composer.lock` auflösen. Die Auswahl ist keine freie Entscheidung des Umsetzenden, sondern folgt dieser verbindlichen, in genau dieser Reihenfolge anzuwendenden Regel: **erstens** keine neue Abhängigkeit, wenn der aufgelöste Abhängigkeitsbaum die Fähigkeit bereits trägt; **zweitens** andernfalls je Fähigkeit höchstens ein zusätzlicher Kandidat, der kumulativ alle Eignungskriterien erfüllt — er ist unter der in `AI6-001` gebundenen Mindestplattform installierbar, führt die Verifikation vollständig serverseitig und ohne Netzzugriff zur Laufzeit aus, benötigt über die im Image vorhandenen hinaus keine oder genau eine PHP-Erweiterung, die im `Dockerfile` ergänzt und in `README.md` benannt wird, bringt keine transitive Abhängigkeit mit, die zur Laufzeit Netzzugriff oder einen externen Dienst voraussetzt, und liegt in einer veröffentlichten, gepflegten Fassung vor, die die benötigten Verfahren abdeckt; **drittens** bei mehreren geeigneten Kandidaten derjenige mit der kleineren Zahl transitiver Abhängigkeiten, bei Gleichstand der mit dem engeren Funktionsumfang; **viertens** erfüllt kein Kandidat die Kriterien, wird keine Bibliothek aufgenommen und keine eigene Kryptoimplementierung geschrieben — der Fall endet als `needs_human`-Entscheidungsanfrage nach Plan §13.3 und nicht als abgeschwächte Umsetzung. Die angewandte Regelstufe, der gewählte Kandidat, die geprüfte und verworfene Alternative samt Ausschlusskriterium und die aufgelöste Version werden in `README.md` festgehalten; `composer.lock` wird unter der gebundenen Mindestplattform neu aufgelöst.
2. Migrationen für Passkey-Credentials, TOTP-Geheimnisse, Recovery-Codes und `login_confirmations` ergänzen; `login_confirmations` speichert ausschließlich HMAC-Digest, Revision, Empfängerbindung, Ablaufzeitpunkt, Versuchszähler sowie den Zustellzustand mit `queued`, `sent` oder `failed`, dem Zeitpunkt der letzten Zustandsänderung und einem redigierten Fehlerschlüssel — niemals den Klartextcode.
3. In `app/AI6/Auth/` den zentralen `LoginCompletionGate` einführen, durch den Passkey-, Passwort/TOTP- und Recoverypfad gleichermaßen laufen und der als einzige Stelle eine Session vom Vorautorisierungszustand in den autorisierten Zustand überführt.
4. Passkey-Registrierung und -Anmeldung mit serverseitig erzeugter Challenge, Origin- und Relying-Party-Prüfung sowie Signaturzählerprüfung umsetzen; abgelehnte Ceremonies erzeugen keine Session und keinen verwertbaren Zustand.
5. TOTP-Einrichtung und -Prüfung mit begrenztem Zeitfenster und Wiederverwendungsschutz innerhalb desselben Fensters umsetzen. Das TOTP-Geheimnis wird zur Verifikation wieder benötigt und deshalb nicht gehasht, sondern mit authentifizierter Verschlüsselung im Ruhezustand abgelegt; Recovery-Codes werden dagegen ausschließlich gehasht gespeichert, sind einmalig einlösbar und ausschließlich über das Kommando aus Aufgabe 6 neu erzeugbar.
6. Das vom Blueprint verlangte Recovery-CLI als Kommando `ai6:reissue-recovery-codes` (neu) vollständig festlegen, weil es als einziger Weg Recovery-Codes ausgibt und dabei Klartextgeheimnisse erzeugt. **Zielbenutzer** — genau einer, über seine E-Mail-Adresse benannt wie bei `ai6:create-admin` aus `AI6-004`; einen Sammel- oder Alle-Benutzer-Modus gibt es nicht. **Autorisierung** — die Grenze ist der lokale Shellzugang zum Container: Es entsteht weder eine HTTP-Route noch ein Queue-Job noch ein sonstiger entfernter Auslöser, und wer das Kommando ausführen kann, besitzt bereits Serverzugang. Eine Step-up-Prüfung nach `AC-13` findet ausdrücklich nicht statt und wird auch nicht simuliert, weil das Kommando ohne Session läuft; ein nachgebildeter Step-up wäre eine Scheinsicherung. **Abgrenzung zum Faktor-Reset** — das Kommando lehnt jeden Benutzer ab, für den weder ein Passkey noch ein TOTP-Geheimnis registriert ist, sowie jeden deaktivierten oder gelöschten Benutzer. Andernfalls wäre es genau der administrative Faktor-Reset, der unter `## Out of Scope` steht: Ein frischer Satz Recovery-Codes ist ein vollwertiger Anmeldeweg. **Ausgabe** — die Codes erscheinen genau einmal auf der Standardausgabe, werden nie im Klartext persistiert, nie in eine Log- oder Datei­senke geschrieben und über keine Option in eine Datei umgeleitet; ein zweiter Aufruf zeigt die vorherigen Codes nicht erneut an, weil ausschließlich ihre Hashes existieren. **Atomarität und Fehler** — Invalidierung des alten Satzes und Ausgabe des neuen geschehen in einer Transaktion; eine unbekannte Adresse, ein deaktivierter Benutzer und ein Benutzer ohne starkes Verfahren enden mit einem von 0 verschiedenen Exitcode ohne Zustandsänderung und ohne Teilausgabe. **Audit** — jeder erfolgreiche Lauf schreibt einen redigierten Auditeintrag aus Zeitpunkt, Zielbenutzer, Anzahl ausgegebener Codes und dem Vermerk der lokalen Ausführung, niemals einen Code oder dessen Hash.
7. Genau **einen** Enrollment-Weg einführen, damit ein Benutzer ohne registriertes starkes Verfahren überhaupt eines einrichten kann — die passwortgebundene Enrollment-Sitzung. Ein zweiter, administrativ ausgestellter Nachweis entsteht hier nicht; er steht unter `## Out of Scope`. Der Vertrag dieses einen Weges ist vollständig festgelegt: **Autorisierung** — die Sitzung entsteht ausschließlich nach korrektem Passwort desselben Benutzers und ausschließlich dann, wenn für ihn kein starkes Verfahren registriert ist; ein Benutzer mit mindestens einem registrierten Verfahren erreicht sie nie. **Berechtigung** — sie erreicht ausschließlich die Registrierungsrouten für Passkey und TOTP, trägt keinerlei Anwendungsberechtigung und ist keine autorisierte Session; `LoginCompletionGate` überführt sie nicht. **Starkes Verfahren** — als starkes Verfahren zählen ausschließlich ein registrierter Passkey und ein eingerichtetes TOTP-Geheimnis. Recovery-Codes sind ausdrücklich **kein** starkes Verfahren: Sie beenden den Enrollment-Zustand nicht, und ihre Ausgabe ist aus ihm heraus nicht erreichbar, solange kein Passkey und kein TOTP registriert ist. Andernfalls verließe ein Benutzer den Enrollment-Zustand ohne jedes Primärverfahren und wäre anschließend ausgesperrt. Recovery-Codes entstehen deshalb erst, wenn mindestens ein starkes Verfahren existiert, und ausschließlich über das Kommando aus Aufgabe 6; eine Selbstbedienungsroute für ihre Ausgabe entsteht in diesem Ticket nicht, weil der Blueprint dafür ein CLI vorsieht. Ein Benutzer besitzt nach abgeschlossenem Enrollment also so lange keine Recovery-Codes, bis ein Betreiber sie ausgibt — das ist die bewusste Folge dieses Zuschnitts und kein Versehen. **Ablauf** — sie endet nach einer in `config/ai6.php` konfigurierten kurzen Frist, spätestens aber mit der ersten erfolgreichen Registrierung eines Passkeys oder eines TOTP-Geheimnisses. **Widerrufbarkeit** — Deaktivieren oder Löschen des Benutzers, ein Passwortwechsel und ein Logout beenden sie sofort; sie ist an genau eine Browsersession gebunden und in keiner anderen einlösbar. **Auditierung** — Entstehung, Ablauf, Widerruf und erfolgreicher Abschluss erzeugen je einen redigierten Auditeintrag aus Benutzer, Zeitpunkt und Ereignis, ohne Faktormaterial. **Step-up** — der Enrollment-Zustand erfüllt keine Step-up-Anforderung nach `AC-13` und ersetzt weder die Primärauthentifizierung noch die E-Mail-Barriere: Nach abgeschlossener Registrierung gilt die reguläre Anmeldung samt E-Mail-Codebestätigung unverändert.
8. Die E-Mail-Codebestätigung umsetzen: achtstelliger Code, Versand über die Mailqueue ausschließlich an die aus Env beziehungsweise `config/ai6.php` aufgelöste Bestätigungsadresse, Eingabe im selben Browser, Versuchslimit, Ablaufzeit und terminale Invalidierung nach Ausschöpfung der Versuche. Der Mailjob wird als verschlüsselte Queue-Payload eingestellt, sodass der Klartextcode auch nicht in der Jobtabelle der Database Queue steht; der Job führt Challenge-ID und Revision mit und aktualisiert nach dem Versandversuch den Zustellzustand.
9. Resend mit neuer Revision umsetzen, die die vorherige Revision sofort ungültig macht; ein bereits eingestellter Mailjob einer älteren Revision darf keine ältere Revision reaktivieren, und ein Wechsel der Empfängeradresse invalidiert alle offenen Challenges.
10. Fail-closed-Verhalten sicherstellen: Schlägt der Mailversand fehl oder ist die Bestätigungsadresse nicht auflösbar, entsteht bei aktiver Maßnahme keine autorisierte Session; der Fehler ist im Panel sichtbar und redigiert protokolliert.
11. Die Step-up-Primitive umsetzen: Eine als kritisch markierte Aktion verlangt bei aktiver Maßnahme eine frische Wiederholung der starken Primärauthentifizierung innerhalb eines konfigurierten Zeitfensters; die Bestätigung wird an Benutzer, Session und Aktionstyp gebunden.
12. Neue Bindungen, Middleware, Kommandos und Routen in `app/AI6/Shared/AI6ServiceProvider.php`, `bootstrap/app.php` und `routes/web.php` registrieren; Ansichten für Codeeingabe, Passkey- und TOTP-Einrichtung unter `resources/views/auth/` sowie die Mailvorlage unter `resources/views/mail/` (neu) ergänzen.
13. Die Codelänge unveränderlich auf genau acht Ziffern festlegen — als Konstante im Code, nicht als Konfigurationswert. Ausschließlich Ablaufzeit, Versuchslimit, Resend-Sperre und Step-up-Fenster kommen als Grenzwerte in den Auth-Abschnitt von `config/ai6.php` und mit sicheren Defaults in `.env.example`, ohne einen echten Empfänger oder Zugangswert aufzunehmen.
14. Tests unter `tests/Unit/Auth/` und `tests/Feature/Auth/` ergänzen, einschließlich der Negativpfade für Code, Revision, Fremdsession, Mailausfall, nicht auflösbare Adresse und rohe Queue-Payload.
15. Das für die WebAuthn-Ceremony nötige Browserskript als selbst gehostete statische Datei unterhalb des öffentlichen Asset-Pfades ausliefern; die Auth-Ansichten enthalten weder Inline-Skripte noch Inline-Eventhandler noch eine externe Skriptquelle, damit sie unter der Content-Security-Policy aus `AI6-005B` ohne Ausnahme laden.
16. `README.md` um Loginablauf, Bestätigungsadresse, Enrollment-Zustand, den Wiederherstellungspfad samt Aufruf, Vorbedingung, Ausgabeverhalten und Auditierung von `ai6:reissue-recovery-codes`, Step-up-Fenster, den Asset-Vertrag der Auth-Ansichten und die dokumentierte Abschaltbarkeit über die `SecurityPolicy` erweitern.

## Acceptance Criteria

- [ ] **AC-01** Im Profil `strict` besitzt eine Session nach erfolgreicher Primärauthentifizierung den Vorautorisierungszustand: Jeder geschützte Endpunkt führt zur Codeeingabe, und erst nach Eingabe eines gültigen Codes existiert eine autorisierte Session.
- [ ] **AC-02** Der Bestätigungscode besteht aus genau acht Ziffern — die Länge ist eine Codekonstante und über keinen Konfigurations- oder Umgebungswert veränderbar — und wird ausschließlich an die aus Env beziehungsweise `config/ai6.php` aufgelöste Adresse gesendet; in `login_confirmations` stehen nur HMAC-Digest, Revision, Empfängerbindung, Ablaufzeitpunkt, Versuchszähler und Zustellzustand, und der Klartextcode erscheint in keiner Datenbankspalte und in keiner Logzeile.
- [ ] **AC-03** Ein falscher, abgelaufener oder bereits eingelöster Code erzeugt keine autorisierte Session; nach der konfigurierten Zahl von Fehlversuchen ist die Challenge terminal ungültig und eine neue Primärauthentifizierung erforderlich.
- [ ] **AC-04** Ein für Session A erzeugter Code lässt sich in keiner anderen Vorautorisierungssession einlösen.
- [ ] **AC-05** Ein Resend erzeugt eine neue Revision und macht die vorherige sofort ungültig; ein bereits eingestellter Mailjob einer älteren Revision kann diese nicht reaktivieren.
- [ ] **AC-06** Ein Wechsel der konfigurierten Bestätigungsadresse macht alle offenen Challenges sofort ungültig.
- [ ] **AC-07** Bei fehlgeschlagenem Mailversand oder nicht auflösbarer Bestätigungsadresse entsteht bei aktiver Maßnahme keine autorisierte Session; der Zustellzustand der betroffenen Challenge steht auf `failed` mit redigiertem Fehlerschlüssel, ist im Panel sichtbar und ohne Secretwert protokolliert.
- [ ] **AC-08** Die Bestätigungsmail enthält den Code und keinen Link, der eine Freigabe auslöst; es existiert kein Endpunkt, der eine Session allein durch Aufruf einer URL autorisiert.
- [ ] **AC-09** `LoginCompletionGate` ist die einzige Stelle, die eine Session vom Vorautorisierungszustand in den autorisierten Zustand überführt; Passkey-, Passwort/TOTP- und Recoverypfad laufen ausnahmslos durch ihn.
- [ ] **AC-10** Eine gültige Passkey-Ceremony mit korrekter Challenge, Origin und Relying Party führt zur Primärauthentifizierung; eine Ceremony mit fremder Challenge, abweichender Origin oder rückläufigem Signaturzähler wird abgelehnt und erzeugt keine Session.
- [ ] **AC-11** Ein korrekter TOTP-Code innerhalb des konfigurierten Fensters führt zur Primärauthentifizierung; derselbe Code wird innerhalb desselben Fensters kein zweites Mal akzeptiert.
- [ ] **AC-12** Recovery-Codes werden ausschließlich gehasht gespeichert, sind genau einmal einlösbar, und eine Neuausgabe über `ai6:reissue-recovery-codes` macht alle vorherigen Codes ungültig. Ihre Ausgabe entsteht ausschließlich über dieses Kommando; es existiert keine HTTP-Route und kein Queue-Job, der Recovery-Codes ausgibt oder neu erzeugt.
- [ ] **AC-13** Eine als kritisch markierte Aktion verlangt bei aktiver Step-up-Maßnahme eine frische Wiederholung der starken Primärauthentifizierung innerhalb des konfigurierten Zeitfensters; eine fehlende oder abgelaufene Bestätigung führt zur Ablehnung ohne Wirkung.
- [ ] **AC-14** Die Abschaltung der E-Mail-Codebestätigung ist ausschließlich über die `SecurityPolicy` aus `AI6-003` möglich, verlangt das dortige Reduktions-Acknowledgement und erscheint in `ai6:doctor` sowie in den Bannerdaten als reduzierte Maßnahme.
- [ ] **AC-15** Der Klartextcode erscheint zu keinem Zeitpunkt in der Jobtabelle der Database Queue: Der Mailjob ist als verschlüsselter Job deklariert, die serialisierte Hülle enthält kein Klartextfeld mit dem Code und nicht den vollständigen Code, und der Payloadinhalt ist ausschließlich über den vorgesehenen Entschlüsselungspfad lesbar.
- [ ] **AC-16** Die Bibliotheksauswahl folgt nachvollziehbar der Entscheidungsregel aus Aufgabe 1 und nicht dem Ermessen des Umsetzenden. Bringt der reale Abhängigkeitsbaum eine Fähigkeit bereits mit, bleiben `composer.json` und `composer.lock` für diese Fähigkeit gegenüber dem Base-Snapshot unverändert. Führt die Umsetzung zu einer neuen Abhängigkeit, ist es je Fähigkeit höchstens eine, sie erfüllt alle kumulativen Eignungskriterien — Installierbarkeit unter der gebundenen Mindestplattform, vollständig serverseitige Verifikation ohne Laufzeit-Netzzugriff, höchstens eine zusätzliche und im `Dockerfile` ergänzte PHP-Erweiterung, keine transitive Abhängigkeit mit Laufzeit-Netzbedarf, veröffentlichte gepflegte Fassung mit den benötigten Verfahren —, `README.md` benennt angewandte Regelstufe, aufgelöste Version, verworfene Alternative samt Ausschlusskriterium und den Tie-Breaker, und `composer.lock` bleibt unter der gebundenen Mindestplattform installierbar. Erfüllt kein Kandidat die Kriterien, entsteht weder eine Abhängigkeit noch eine eigene Kryptoimplementierung, sondern eine `needs_human`-Entscheidungsanfrage; eine selbst geschriebene WebAuthn- oder TOTP-Verifikation ist in jedem Fall ein Fehler.
- [ ] **AC-17** Es existiert genau ein Enrollment-Weg, und sein Vertrag ist vollständig bestimmt. Die Enrollment-Sitzung entsteht ausschließlich nach korrektem Passwort und ausschließlich für einen Benutzer ohne registriertes starkes Verfahren; ein Benutzer mit mindestens einem registrierten Verfahren erhält sie nie. Sie erreicht ausschließlich die Registrierungsrouten für Passkey und TOTP, jede andere Route und jede Anwendungsaktion wird abgewiesen, und es entsteht keine autorisierte Session. Als starkes Verfahren zählen ausschließlich ein registrierter Passkey und ein eingerichtetes TOTP-Geheimnis; Recovery-Codes zählen nicht, beenden den Enrollment-Zustand nicht und sind aus ihm heraus nicht ausgebbar, solange kein Passkey und kein TOTP registriert ist — ein Codepfad, der einen Benutzer allein über Recovery-Codes aus dem Enrollment entlässt, ist ein Fehler, weil er ihn ohne Primärverfahren aussperrt. Sie endet mit der ersten erfolgreichen Registrierung eines Passkeys oder TOTP-Geheimnisses, spätestens nach der konfigurierten Frist, und wird zusätzlich durch Deaktivierung, Löschung, Passwortwechsel und Logout sofort ungültig; sie ist an genau eine Browsersession gebunden und in keiner anderen einlösbar. Entstehung, Ablauf, Widerruf und Abschluss sind redigiert auditiert. Sie erfüllt keine Step-up-Anforderung nach `AC-13`, und nach der Registrierung gilt die reguläre Anmeldung samt E-Mail-Barriere unverändert. Ein zweiter Enrollment-Pfad — insbesondere ein administrativ ausgestellter Nachweis — existiert in diesem Ticket nicht; ein solcher Codepfad ist ein Fehler.
- [ ] **AC-18** Das TOTP-Geheimnis liegt mit authentifizierter Verschlüsselung im Ruhezustand vor: In der Datenbank steht kein Klartextgeheimnis, eine manipulierte Chiffre wird beim Entschlüsseln abgewiesen statt still verworfen, und das Geheimnis erscheint in keinem Log.
- [ ] **AC-19** Die Auth-Ansichten enthalten kein Inline-Skript, keinen Inline-Eventhandler und keine externe Skriptquelle; das WebAuthn-Browserskript wird als selbst gehostete statische Datei ausgeliefert.
- [ ] **AC-20** Das Kommando `ai6:reissue-recovery-codes` erfüllt seinen vollständigen Vertrag aus Aufgabe 6. Es benennt genau einen Zielbenutzer über dessen E-Mail-Adresse und kennt keinen Sammelmodus. Es ist ausschließlich lokal ausführbar: Weder eine HTTP-Route noch ein Queue-Job noch ein sonstiger entfernter Auslöser erreicht es, und eine Step-up-Prüfung findet mangels Session ausdrücklich nicht statt und wird auch nicht nachgebildet. Es lehnt jeden Benutzer ohne registrierten Passkey und ohne TOTP-Geheimnis sowie jeden deaktivierten oder gelöschten Benutzer mit einem von 0 verschiedenen Exitcode ab, sodass es kein Ersatz für den unter `## Out of Scope` stehenden administrativen Faktor-Reset ist. Die erzeugten Codes erscheinen genau einmal auf der Standardausgabe; sie stehen in keiner Logzeile, keiner Datei und keiner Datenbankspalte im Klartext, das Kommando besitzt keine Option zur Umleitung in eine Datei, und ein zweiter Aufruf zeigt einen früheren Satz nicht erneut an. Invalidierung des alten Satzes und Erzeugung des neuen erfolgen in einer Transaktion; jeder Fehlerfall endet ohne Zustandsänderung und ohne Teilausgabe. Jeder erfolgreiche Lauf hinterlässt einen redigierten Auditeintrag aus Zeitpunkt, Zielbenutzer, Anzahl der Codes und dem Vermerk der lokalen Ausführung, der weder einen Code noch dessen Hash enthält.

## Test Cases

- **TC-01** Feature-Test: Nach korrekter Primärauthentifizierung im strict-Profil liefert jeder geschützte Endpunkt die Codeeingabe statt Inhalt; nach Eingabe des gültigen Codes ist die Session autorisiert.
- **TC-02** Feature-Test: Der erzeugte Code ist achtstellig, geht ausschließlich an die konfigurierte Adresse, und in der Datenbankzeile sowie im Log erscheint kein Klartextcode.
- **TC-03** Feature-Test: Falscher, abgelaufener und bereits eingelöster Code erzeugen keine autorisierte Session; nach Ausschöpfung der Versuche ist die Challenge terminal ungültig und eine neue Primärauthentifizierung erforderlich.
- **TC-04** Feature-Test: Der in Session A erzeugte Code wird in Session B abgewiesen und bleibt in Session A weiterhin einlösbar, solange er gültig ist.
- **TC-05** Feature-Test: Nach einem Resend ist die vorherige Revision sofort ungültig, und ein parallel zugestellter Mailjob der alten Revision autorisiert keine Session.
- **TC-06** Feature-Test: Nach Wechsel der konfigurierten Bestätigungsadresse werden alle offenen Challenges abgewiesen.
- **TC-07** Feature-Test: Ein Fehler des Mailtransports führt zu keiner autorisierten Session; der Fehler ist im Ergebnis sichtbar und im Log ohne Secretwert enthalten.
- **TC-08** Feature-Test: Die gerenderte Mail enthält den Code und keinen Bestätigungslink; keine GET-Route schließt einen Login ab.
- **TC-09** Unit-Test: Ein Architekturtest weist nach, dass ausschließlich `LoginCompletionGate` den Übergang in den autorisierten Sessionzustand ausführt.
- **TC-10** Feature-Test: Passkey-Registrierung und -Anmeldung mit deterministischen Ceremony-Fixtures gelingen; Fixtures mit fremder Challenge, abweichender Origin und rückläufigem Signaturzähler werden abgewiesen.
- **TC-11** Feature-Test: Ein gültiger TOTP-Code authentifiziert; derselbe Code im selben Fenster und ein Code außerhalb des Fensters werden abgewiesen.
- **TC-12** Feature-Test: Ein Recovery-Code ist genau einmal einlösbar, in der Datenbank nur gehasht vorhanden, und nach Neuausgabe sind alle vorherigen Codes ungültig.
- **TC-13** Feature-Test: Eine als kritisch markierte Aktion wird ohne frische Step-up-Bestätigung und mit abgelaufener Bestätigung abgelehnt und nach frischer Bestätigung ausgeführt.
- **TC-14** Feature-Test: Bei über die `SecurityPolicy` abgeschalteter und bestätigter E-Mail-Bestätigung entsteht die autorisierte Session ohne Code, und `ai6:doctor` sowie die Bannerdaten weisen die Reduktion aus; ohne Acknowledgement startet die Anwendung nicht.
- **TC-15** Feature-Test: Nach dem Einstellen des Mailjobs wird die rohe Payloadspalte der Jobtabelle strukturell geprüft: Der Job ist als verschlüsselter Job deklariert, die Hülle enthält kein Klartextfeld mit dem Code und nicht den vollständigen Code, und die Entschlüsselung gelingt nur über den vorgesehenen Pfad. Ein absichtlich unverschlüsselt eingestellter Vergleichsjob lässt den Testfall fehlschlagen. Der Test vergleicht bewusst keine kurzen Teilzeichenketten, weil eine Zufallsfolge im Chiffrat sie zufällig enthalten kann.
- **TC-16** Feature-Test: Der Zustellzustand einer Challenge wechselt von `queued` über `sent` bei Erfolg beziehungsweise auf `failed` mit redigiertem Fehlerschlüssel bei Transportfehler; ein Resend erzeugt eine neue Revision mit eigenem Zustellzustand, ohne den Zustand der alten Revision zu überschreiben.
- **TC-17** Tooling-Test: Sind beide Fähigkeiten bereits vorhanden, entsprechen die Hashes von `composer.json` und `composer.lock` dem Base-Snapshot; andernfalls enthält `composer.json` je Fähigkeit höchstens einen neuen Eintrag, `composer.lock` ist unter der gebundenen Mindestplattform installierbar, und `README.md` nennt Version, geprüfte Alternative und Auswahlgrund.
- **TC-18** Feature-Test: Ist die Bestätigungsadresse nicht auflösbar — weder in Env noch in `config/ai6.php` gesetzt beziehungsweise kein gültiger Adresswert —, entsteht bei aktiver Maßnahme keine autorisierte Session, der Zustellzustand der Challenge steht auf `failed` mit redigiertem Fehlerschlüssel, und weder Antwort noch Log enthalten einen Secretwert. Dieser Pfad wird getrennt vom Mailtransportfehler aus TC-07 geprüft und schlägt fehl, sobald der Mailversand überhaupt versucht wird.
- **TC-19** Feature-Test des einen Enrollment-Weges, je Zusage getrennt: Ein Benutzer ohne starkes Verfahren erhält nach korrektem Passwort eine Enrollment-Sitzung, erreicht damit ausschließlich die Registrierungsrouten und wird auf jeder anderen Route sowie bei jeder Anwendungsaktion abgewiesen. Ein Benutzer mit bereits registriertem Verfahren erhält sie nicht. Recovery-Codes werden als eigener Fall geprüft: Aus dem Enrollment-Zustand heraus ist ihre Ausgabe abgewiesen, solange weder Passkey noch TOTP registriert ist, und ein Testdoppel, das den Zustand allein durch Recovery-Codes beendet, lässt den Testfall fehlschlagen; nach registriertem Passkey oder TOTP gelingt die Ausgabe in der regulären autorisierten Session. Nach erfolgreicher Registrierung eines Passkeys oder TOTP-Geheimnisses ist der Zustand beendet, ebenso nach Ablauf der konfigurierten Frist sowie nach Deaktivierung, Löschung, Passwortwechsel und Logout; in einer fremden Browsersession ist sie nicht einlösbar. Die Enrollment-Sitzung erfüllt keine Step-up-Prüfung, und nach der Registrierung verlangt die reguläre Anmeldung weiterhin die E-Mail-Codebestätigung. Entstehung, Ablauf, Widerruf und Abschluss erscheinen redigiert im Audit, ohne Faktormaterial. Ein Architekturtest weist genau einen Enrollment-Einstiegspunkt nach und lässt jeden zweiten Pfad — insbesondere ein Kommando oder eine Route, die einen Enrollment-Nachweis ausstellt — fehlschlagen.
- **TC-20** Unit-Test: Das gespeicherte TOTP-Geheimnis ist weder Klartext noch ein blosser Hash; eine um ein Byte veränderte Chiffre führt beim Entschlüsseln zu einem benannten Authentizitätsfehler, und weder Ergebnis noch Log enthalten das Geheimnis.
- **TC-21** Feature-Test: Die gerenderten Auth-Ansichten enthalten kein `<script>` mit Inline-Inhalt, kein `on*`-Attribut und keine Skriptquelle außerhalb des eigenen Asset-Pfades.
- **TC-22** Unit-Test: Die Codelänge ist eine Konstante; es existiert kein Konfigurations- oder Umgebungsschlüssel, der sie verändert, und ein erzeugter Code besteht aus genau acht Ziffern.
- **TC-23** Feature- und Negativtest des Kommandos `ai6:reissue-recovery-codes`, je Zusage getrennt: Für einen aktiven Benutzer mit registriertem Passkey gibt es die Codes genau einmal auf der Standardausgabe aus, invalidiert den vorherigen Satz und legt genau die Hashes des neuen Satzes ab; die aufgezeichnete Kommandoausgabe erscheint in keiner Logzeile, und weder Log noch Datenbank enthalten einen Code im Klartext. Ein zweiter Aufruf gibt einen neuen Satz aus und zeigt den vorherigen nicht erneut. Abgewiesen mit einem von 0 verschiedenen Exitcode und ohne jede Zustandsänderung werden: eine unbekannte E-Mail-Adresse, ein deaktivierter Benutzer und ein Benutzer, für den weder Passkey noch TOTP registriert ist — der Testfall weist für den letzten Fall ausdrücklich nach, dass das Kommando keinen Wiederherstellungsweg für einen Benutzer ohne starkes Verfahren eröffnet. Ein erzwungener Fehler zwischen Invalidierung und Ausgabe hinterlässt weder einen halb invalidierten alten noch einen halb erzeugten neuen Satz. Der Auditeintrag enthält Zeitpunkt, Zielbenutzer und Anzahl, aber weder Code noch Hash. Ein Architekturtest weist nach, dass keine registrierte Route und kein Queue-Job Recovery-Codes erzeugt oder ausgibt und dass das Kommando keine Option zur Dateiausgabe besitzt.

## AC Coverage

| AC | Evidence |
|---|---|
| AC-01 | TC-01 |
| AC-02 | TC-02, TC-22 |
| AC-03 | TC-03 |
| AC-04 | TC-04 |
| AC-05 | TC-05 |
| AC-06 | TC-06 |
| AC-07 | TC-07, TC-16, TC-18, MG-02 |
| AC-08 | TC-08, MG-02 |
| AC-09 | TC-09 |
| AC-10 | TC-10, MG-01 |
| AC-11 | TC-11 |
| AC-12 | TC-12, TC-23 |
| AC-13 | TC-13 |
| AC-14 | TC-14 |
| AC-15 | TC-15 |
| AC-16 | TC-17 |
| AC-17 | TC-19 |
| AC-18 | TC-20 |
| AC-19 | TC-21 |
| AC-20 | TC-23 |

## Initial Scope and Sensitive Paths

**Expected initial scope:**

- `composer.json` — existing
- `composer.lock` — existing
- `Dockerfile` — existing
- `database/migrations/` — existing
- `app/AI6/Auth/` — existing
- `app/AI6/Shared/AI6ServiceProvider.php` — existing
- `config/ai6.php` — existing
- `config/auth.php` — existing
- `config/mail.php` — existing
- `bootstrap/app.php` — existing
- `routes/web.php` — existing
- `resources/views/auth/` — existing
- `resources/views/mail/` — new
- `public/` — existing
- `.env.example` — existing
- `tests/Unit/Auth/` — existing
- `tests/Feature/Auth/` — existing
- `README.md` — existing

**Sensitive paths:**

- `composer.json` — Dependency-Vertrag; erwartete Entscheidung: genehmigt für höchstens eine zusätzliche Bibliothek je Fähigkeit, ausgewählt gegen den realen Lockstand und mit dokumentierter Begründung; keine Aufnahme, wenn die Fähigkeit bereits vorhanden ist.
- `composer.lock` — exakte Supply-Chain-Baseline; erwartete Entscheidung: genehmigt für die daraus folgende Neuauflösung unter der gebundenen Mindestplattform, mit sichtbarem Diff.
- `Dockerfile` — Laufzeitimage; erwartete Entscheidung: genehmigt ausschließlich für eine von der gewählten Bibliothek geforderte PHP-Erweiterung.
- `database/migrations/` — Migrationskategorie nach Plan §6.2; erwartete Entscheidung: genehmigt für Credential-, Geheimnis- und Challenge-Tabellen ohne Klartextspeicherung.
- `app/AI6/Auth/` — Authentifizierungspfad und einzige Übergangsstelle in den autorisierten Sessionzustand; erwartete Entscheidung: genehmigt, mit ausdrücklicher Prüfung der Fail-closed-Pfade.
- `config/mail.php` — Zustellweg der Sicherheitsmails; erwartete Entscheidung: genehmigt, ohne echten Zugangswert im Repository.
- `routes/web.php` — Angriffsfläche der Weboberfläche; erwartete Entscheidung: genehmigt, sofern keine Route eine Session allein durch Aufruf autorisiert.
- `public/` — öffentlich ausgelieferter Asset-Pfad; erwartete Entscheidung: genehmigt ausschließlich für das selbst gehostete WebAuthn-Browserskript, ohne externe Quelle und ohne Buildschritt.
- `.env.example` — Vorlage der Sicherheitsdefaults; erwartete Entscheidung: genehmigt, sofern kein echter Empfänger, Schlüssel oder Zugangswert aufgenommen wird.

## Do Not Change

- `app/AI6/Shared/Redaction/` — einzige Redaction-Regelauflösung aus `AI6-003`; dieses Ticket konsumiert sie und erweitert sie nicht.
- `app/AI6/Shared/Security/` — Profil-, Schalter- und Hashvertrag aus `AI6-003`; dieses Ticket verwendet die vorhandenen Schalter, ohne neue einzuführen.
- `app/AI6/Shared/Http/` — Cookie-, Host-, Proxy- und CSP-Vertrag gehört `AI6-005B`.
- `app/AI6/Shared/Markdown/` — Darstellungspolitik gehört `AI6-005B`.
- `config/session.php` — Sessioncookie-Vertrag gehört `AI6-005B`.
- `app/AI6/Projects/` — Rollen- und Mitgliedschaftsmodell aus `AI6-004` bleibt unverändert.
- `docker-compose.yml` — Rollen- und Netzwerkbild aus `AI6-002`.
- `docs/AI6_IMPLEMENTATION_PLAN.md` — normative Quelle.
- `docs/AI6_TICKET_TEMPLATE_V1.md` — Erzeugungs- und Umsetzungsvertrag.
- `AGENTS.md` — freigegebene Instruktionsquelle.
- `CLAUDE.md` — importiert ausschließlich `AGENTS.md`.
- `.gitattributes` — Zeilenendenvertrag und damit Teil der Blob-SHA-Stabilität.
- `tickets/` — Ticketinhalt und Ticketstatus werden vom Implementierungsagenten nicht verändert.

## Out of Scope

- Ein zweiter, administrativ ausgestellter Enrollment-Nachweis und jeder administrative Reset registrierter Faktoren für einen Benutzer, der Passkey, TOTP und Recovery-Codes gemeinsam verloren hat. Beides wäre ein zweiter Weg zu einer Identität und verlangt eine eigene Entscheidung über Autorisierung, Ablauf, Widerruf, Auditierung und Step-up; für den Erstzugang wird es nicht benötigt, weil ein angelegter Benutzer über sein Passwort selbst in den Enrollment-Zustand kommt. Der Bedarf ist eine Entscheidungsanfrage nach Plan §13.3 und kein stiller Ausbau dieses Tickets. Das Kommando `ai6:reissue-recovery-codes` aus Aufgabe 6 ist davon abgegrenzt und kein verdeckter Ersatz: Es setzt ein bereits registriertes starkes Verfahren voraus und weist einen Benutzer ohne Passkey und ohne TOTP ausdrücklich ab (`AC-20`). Ein Kommando, das diese Vorbedingung fallen ließe, wäre genau der hier ausgeschlossene Faktor-Reset, weil ein frischer Satz Recovery-Codes ein vollwertiger Anmeldeweg ist.
- Cookie-, Host-, Proxy-, CSP- und Markdown-Härtung (`AI6-005B`).
- Einrichtung von VPN, HTTPS-Terminierung mit öffentlichem Zertifikat oder SSH-Tunnel (`AI6-036`).
- Human-Request-Mails an den verantwortlichen Benutzer und die Attention-Inbox (`AI6-018`).
- Projekte, Remote, Control-Branch und Git-Zugriff; die hier eingeführte Step-up-Primitive wird beim autorisierten Control-Branch-Wechsel erstmals fachlich aufgerufen (`AI6-006E`).
- Provider-Credentials, Credentialprojektionen und Capability-Doctor (`AI6-035`).
- Doctor-Prüfungen für Mailtransport im Betriebskontext sowie Backup, Restore und Rotationsverfahren (`AI6-036`).
- Retention und Löschung abgelaufener Sicherheits- und Mailprotokolle (`AI6-036`).

## Manual and External Gates

- **MG-01** Ein Mensch registriert und verwendet einen realen Passkey in einem Browser über eine gesicherte Verbindung und prüft anschließend Anmeldung, Abmeldung und erneute Anmeldung. Evidenz: Abnahmeprotokoll mit Browser, Authenticator-Typ, Zeitpunkt und Ergebnis je Schritt, gebunden an den geprüften Git-Commit oder — vor einem Commit — an Base-Commit und SHA-256 des vollständigen Prüfdiffs.
- **MG-02** Ein Mensch führt einen realen Loginversuch mit tatsächlichem Mailversand an die konfigurierte Bestätigungsadresse durch, bestätigt Zustellung, achtstelligen Code und das Fehlen jedes wirksamen Links und wiederholt den Versuch mit absichtlich fehlerhaftem Mailtransport, ohne dass eine autorisierte Session entsteht. Evidenz: Abnahmeprotokoll mit Zeitpunkt, redigierten Mailkopfzeilen ohne Code, beobachtetem Verhalten in beiden Fällen und derselben Commit- beziehungsweise Diff-Bindung wie in MG-01.

## Review Focus

- Genau eine Übergangsstelle in den autorisierten Sessionzustand; kein Nebenpfad autorisiert eine Session.
- Fail-closed bei Mailfehler, fehlender Adresse, abgelaufener Challenge und ausgeschöpften Versuchen.
- Kein Klartextcode und kein Geheimnis in Datenbank, Log, Exception oder Mailprotokoll; die Jobtabelle der Database Queue zählt dabei ausdrücklich als Datenbank, weshalb die Mailpayload verschlüsselt eingestellt wird.
- Bindung von Code, Revision und Session ist kryptografisch, nicht nur durch Reihenfolge; der Zustellzustand ist je Revision geführt.
- Kein wirksamer Link in der Mail und keine Route, die allein durch Aufruf autorisiert.
- Passkey-Prüfung bindet Challenge, Origin, Relying Party und Signaturzähler; TOTP-Wiederverwendung im selben Fenster wird abgewiesen.
- Recovery-Codes sind einmalig, gehasht und vollständig invalidierbar; das TOTP-Geheimnis ist dagegen authentifiziert verschlüsselt, weil Hashing dort unmöglich ist.
- Das Recovery-CLI ist die einzige Stelle dieses Tickets, die ein Anmeldegeheimnis im Klartext ausgibt. Geprüft wird deshalb beides: dass die Codes den Terminalkanal nicht verlassen — keine Logzeile, keine Datei, keine Option zur Umleitung — und dass die Vorbedingung eines registrierten starken Verfahrens tatsächlich erzwungen wird. Ohne sie wäre das Kommando der administrative Faktor-Reset, den `## Out of Scope` ausschließt, nur unter anderem Namen.
- Die Autorisierung des CLI ist der lokale Shellzugang und keine nachgebildete Rollen- oder Step-up-Prüfung. Ein Codepfad, der ein Step-up für eine sessionlose Ausführung simuliert, ist ein Befund: Er erzeugt den Anschein einer Kontrolle, die dort nicht existieren kann.
- Die Enrollment-Sitzung besitzt keinerlei Anwendungsberechtigung und ist kein Seitenkanal an der E-Mail-Barriere vorbei.
- Die Codelänge ist unveränderlich; Konfigurierbarkeit wäre hier eine Abschwächung, keine Flexibilität.
- Step-up ist an Benutzer, Session, Aktionstyp und Zeitfenster gebunden und nicht durch Wiederholung eines alten Nachweises erfüllbar.
- Neue Composer-Abhängigkeiten sind minimal, begründet, gepinnt und im Lockfile sichtbar.

## Notes

- Definition of Done: `docs/AI6_IMPLEMENTATION_PLAN.md` §12.2.
- Ticket status, approval and run metadata are owned by AI6. The implementation agent never changes them.
- Necessary deviations are reported as `needs_human` or as a scope/contract request, never applied silently.
- Dieses Ticket entstand mit Planrevision V1.6.2 aus der Teilung des früheren Blueprints `AI6-005`; die Webhärtung liegt seitdem in `AI6-005B`.
- Dieses Ticket wurde vor der Umsetzung von `AI6-003` und `AI6-004` erzeugt. Die als `existing` markierten Pfade entstehen erst dort und sind heute nicht vorhanden. Vor `status: ready` sind `files`, die Scope-Marker sowie alle genannten Pfade, Klassen, Kommandos und Tests gegen den dann realen Repositoryzustand neu abzuleiten (Plan §0.3, §13.6, Template §9.1, `AGENTS.md` §8.1); die Abhängigkeitsentscheidung aus Aufgabe 1 ist dafür der Hauptpunkt.
- Die Content-Security-Policy entsteht in `AI6-005B`. Dieses Ticket hält seinen Teil des gemeinsamen Vertrags ein, indem es ohne Inline-Skripte auskommt; die gemeinsame Prüfung erfolgt am M0-Integrations-Gate, wenn beide Tickets gelandet sind.
- Mit Planrevision V1.6.17 trägt `AI6-005B` dieses Ticket in seinem `depends_on`; die zuvor hier als verbindlich behauptete Integrationsreihenfolge ist damit strukturell erzwungen und als Begründung in den `## Context` gewandert. Ein Pflichtinhalt in den Notes wäre nach Template §5 und C11 ohnehin unzulässig gewesen.
- Zwei sicherheitsrelevante Entscheidungen sind gegen den unveränderten Plan und Blueprint nachgeholt worden, weil sie für ein `risk: high`-Ticket zu offen standen. Erstens war die Bibliotheksauswahl nur als „höchstens eine je Fähigkeit" begrenzt, ohne Kriterium, wonach entschieden wird; Composer-Scope, Angriffsfläche und `Dockerfile` hingen damit am Ermessen des Umsetzenden. Aufgabe 1 und `AC-16` legen jetzt eine verbindliche vierstufige Regel mit kumulativen Eignungskriterien, Tie-Breaker und fail-closed-Ausgang fest; offen bleibt ausschließlich, welcher Kandidat sie im realen Lockstand erfüllt. Ein konkreter Paketname wird bewusst nicht genannt, weil er heute nicht überprüfbar wäre (`AGENTS.md` §10). Zweitens standen zwei Enrollment-Wege gleichrangig nebeneinander, ohne dass Autorisierung, Ablauf, Widerruf, Auditierung und Step-up-Verhältnis für den administrativen Nachweis bestimmt waren — zwei Wege zu einer Identität mit nur einem definierten Vertrag. Gewählt ist der passwortgebundene Enrollment-Zustand; sein Vertrag ist in Aufgabe 7 und `AC-17` vollständig festgelegt und in `TC-19` je Zusage geprüft, einschließlich eines Architekturtests gegen einen zweiten Einstiegspunkt. Der administrative Nachweis und der Faktor-Reset stehen unter `## Out of Scope` und sind als Entscheidungsanfrage benannt; die Freigabe trägt diese Verengung bewusst mit. Kein `AC-`, `TC-` oder `MG-`-Bezeichner ist hinzugekommen oder umnummeriert worden.
- Anschließend ist der Begriff „starkes Verfahren" für das Ende des Enrollment-Zustands eindeutig gemacht worden. Aufgabe 7 und `AC-17` ließen offen, ob die Registrierung von Recovery-Codes allein bereits genügt; sie hätte einen Benutzer ohne Passkey und ohne TOTP aus dem Enrollment entlassen und ihn damit ausgesperrt, weil die reguläre Anmeldung ein Primärverfahren verlangt. Als starkes Verfahren zählen jetzt ausschließlich Passkey und TOTP; Recovery-Codes beenden den Zustand nicht und sind aus ihm heraus nicht ausgebbar, solange keines von beiden existiert. Die Enrollment-Sitzung erreicht entsprechend nur noch die Registrierungsrouten für Passkey und TOTP, und `TC-19` prüft den Recovery-Code-Fall getrennt mit Gegenprobe. Kein Bezeichner ist hinzugekommen oder umnummeriert worden.
- Anschließend ist das vom Blueprint verlangte Recovery-CLI ausspezifiziert worden. Zuvor stand es nur als Halbsatz in Aufgabe 5 — „über ein Artisan-Kommando neu erzeugbar" — ohne Name, Zielbenutzer, Autorisierung, Ausgabeverhalten, Auditierung und Fehlerbehandlung, während `AC-12` und `TC-12` allein Hashing und Invalidierung prüften. Das ist die einzige Stelle dieses Tickets, die ein Anmeldegeheimnis im Klartext ausgibt, und sie grenzte zugleich unscharf an den unter `## Out of Scope` stehenden administrativen Faktor-Reset: Ein Kommando, das einem Benutzer ohne Passkey und ohne TOTP frische Recovery-Codes ausstellt, ist genau dieser Reset. Die Entscheidung zwischen Selbstbedienung und lokalem Betreiberkommando ist dabei nicht frei gewesen — Plan §15 führt „Recovery-CLI" als Deliverable, sodass ein Ersatz durch eine Weboberfläche eine Blueprintabweichung nach Plan §13.3 wäre. Die neue Aufgabe 6 legt deshalb `ai6:reissue-recovery-codes` vollständig fest, `AC-20` bindet den Vertrag, und `TC-23` prüft Positivfall, die drei Ablehnungsgründe, den Transaktionsabbruch, die Auditzeile und über einen Architekturtest die Abwesenheit jeder Route, jedes Queue-Jobs und jeder Dateiausgabeoption. Die Aufgabenliste ist dadurch um einen Punkt gewachsen und ab Aufgabe 7 neu durchnummeriert; `AC-20` und `TC-23` sind ausschließlich angehängt, und kein bestehender Bezeichner ist umnummeriert worden. Als Folge besitzt ein Benutzer nach dem Enrollment so lange keine Recovery-Codes, bis ein Betreiber sie ausgibt; das steht jetzt ausdrücklich im `## Context`, statt sich hinter einer Selbstbedienungsroute zu verstecken, die dieses Ticket gar nicht liefert.
- Die Bestätigungsadresse aus `SEC-003` ist bewusst eine einzelne Sicherheitsadresse und nicht die Benutzeradresse; die spätere Human-Request-Zustellung nach Plan §8.3 verwendet einen anderen Empfänger.
- Die beiden manuellen Gates bleiben bis zur gebundenen menschlichen Evidenz offen und blockieren nach `RUN-009` Candidate, finalen Commit und Push.
